# تدقيق شامل — كل ملف، كل زاوية

**746/746 اختبار** · ٦٧ ملفاً · ~٢٠٬٠٠٠ سطر

---

## ما فعلتُه

فحصتُ المشروع على **٩ محاور** بدل انتظار بلاغك. كل محور بفحص آلي،
لا برأي.

---

## ١. الترجمة — ٢٠٣ نص كان لا يزال فرنسياً

قلتَ «اللغات لا تعمل» في الجولة الماضية، فأصلحتُ ما ظهر في الشاشات.
لكن التدقيق الآلي وجد **٢٠٣ نصاً** آخر مختبئاً في المسارات التي لا
تراها إلا حين يحدث خطأ:

```
auth_sm.js       ١٢  رسائل فشل تسجيل الدخول
messages_sm.js   ٤٣  تأكيدات وقوائم
profile_sm.js    ٢٥
campus_sm.js     ٢٤
notify_sm.js     ١٠  نصوص الإشعارات نفسها
```

**شاشة الدخول كانت فرنسية بالكامل** — أول ما يراه طالب جديد.

الآن: **٤٩٠ مفتاحاً × ٣ لغات**، تطابق تام، وصفر تسريب عبر العشر شاشات
بالإنجليزية والعربية.

## ٢. خطأ حقيقي: مُعامِل يحجب دالة الترجمة

```js
const mediaLabel = t => ({ image: t('feed.photo'), … }[t] || …)
                   ↑ حجب t المستورَدة
```

تعديل جماعي سابق حوّل النصوص داخل هذا النطاق، فصار كل وصف مرفق
`undefined`. اكتشفتُه حين فشل استبدال على مسافة بيضاء ففتحتُ السطر.
ثم فحصتُ كل الملفات بحثاً عن نظائر — لا يوجد غيره.

## ٣. مفتاح مكرَّر يعني نصاً خاطئاً

```
'notif.follows': 'follows you'   ← السطر ٢٠٤
'notif.follows': 'Follows'       ← السطر ٣٣٧، يفوز
```

فإشعار المتابعة كان يقول «Follows» بدل «فلان يتابعك». **التعريف
الثاني يفوز صامتاً** — لهذا صار فحص التكرار اختباراً دائماً.

## ٤. رسائل الخطأ صارت مفهومة

كان الطالب يرى `Network request failed` — نصّ مكتبة بالإنجليزية داخل
واجهة فرنسية. الآن `errorText()` واحدة:

```
انقطاع الشبكة  →  «لا يوجد اتصال. تحقّق من الشبكة.»
٤٠١            →  «انتهت الجلسة»
٤٠٣            →  «لا تملك صلاحية»
٥٠٠+           →  «الخادم لا يستجيب»
```

ورسالة تقنية كانت تُعرض للطالب («شغّل db/08_fixes_sm.sql») انتقلت إلى
الـconsole، حيث ينفع المطوّر لا الطالب.

## ٥. اختبار انقطاع الشبكة

شغّلتُ التطبيق بواجهة تفشل في **كل** طلب — سيناريو واي‑فاي الجامعة:

```
feed           "Your story · For you · Following…"   [إعادة]
messages       "Could not load · No connection…"     [إعادة]
notifications  …                                     [إعادة]
hub · events · qa · channels · explore               [إعادة]

  أخطاء غير ملتقَطة: صفر
  شاشات بيضاء:      صفر
```

كل شاشة تشرح وتعرض زر إعادة. لا شاشة فارغة، ولا انهيار.

## ٦. ما فحصتُه ووجدتُه سليماً

لم ألمس ما لم يكن مكسوراً:

```
✓ صفر console.log متروك        ✓ صفر TODO/FIXME
✓ صفر innerHTML بلا esc()      ✓ صفر eval
✓ كل مؤقّت يُنظَّف              ✓ URLs تُحرَّر
✓ z-index كلها رموز            ✓ RLS مفروضة
✓ ٩٦ try / ٤٩ catch            ✓ العروض تعمل بصلاحية المستدعي
✓ prefers-reduced-motion       ✓ سجلّ XP للإضافة فقط
✓ :focus-visible               ✓ صفر مفتاح سرّي في config
```

## ٧. الوصول

زرّان بلا تسمية: أحدهما تسميته الوحيدة كانت اختصار «N» — بلا معنى
لقارئ الشاشة. أُصلحا، والآن **٢٧/٢٧** زر أيقونة مُسمّى ومترجَم.

---

## الاختبار الجديد: `tests/quality.test.mjs`

**٣٤ بوابة** على مستوى المشروع كله، لا على ميزة واحدة:

```
تكرار المفاتيح · تطابق اللغات · تسريب النصوص
معالجة الأخطاء · تسريب المؤقّتات · الأمان
الوصول · رموز التصميم · صحّة الهجرات
```

كل بوابة موجودة **لأنها أمسكت شيئاً فعلاً**.

## وثمانية اختبارات قديمة كانت هشّة

كانت تفحص كلمات فرنسية فانكسرت حين تُرجمت الواجهة. لم أخفِّفها —
جعلتُها تفحص السلوك، فصارت **أقوى**:

```js
// قبل
ok('card labelled', label.textContent.includes('Carte'));
// بعد — يعمل بأي لغة، ويفحص وجود الحقل فعلاً
ok('card field is labelled', label.textContent.trim().length > 2);
```

---

## ما لم أفعله — بوضوح

**لم أفتح متصفحاً حقيقياً.** jsdom يبني DOM ولا يحسب تخطيطاً: لا عرض،
لا ارتفاع، لا تداخل، لا تباين محسوب.

هذه بقيت غير مُتحقَّقة:

```
✗ المحاذاة البصرية عند 360 / 768 / 1440
✗ تباين الوضع الداكن بالأرقام
✗ سلاسة الحركات
✗ الأداء تحت ١٠٠٠ رسالة
✗ سلوك متصفحات الهاتف الحقيقية
```

**وشيء لا يستطيع أي اختبار عندي إثباته:** أن كل هذا يعمل مع Neon
الحيّة. الاختبارات تستخدم مُثبِّتاً يحاكي القاعدة. **شغّل الهجرات
`05 → 06 → 07 → 08`** وجرّب، وإن فشل شيء أرسل خطأ الـconsole.

---

## ما بقي مفتوحاً عن قصد

- **أسماء الكليات لا تُترجَم.** «Informatique» اسم كلية جزائرية حقيقية،
  كاسم شخص. لكنها ما زالت مصفوفة في الكود — **الأفضل نقلها إلى
  القاعدة** ليضيف المشرف كليات دون تعديل الكود. لم أفعلها لأنها تغيير
  في المخطط، وأردتُ موافقتك أولاً.
- **الإشعارات ليست Web Push.** تعمل والصفحة مفتوحة فقط. الـpush الحقيقي
  يحتاج خدمة دفع ومفاتيح VAPID وخادماً — قرار منفصل.
