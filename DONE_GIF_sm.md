# GIF حقيقية — لا، لم أفعلها من قبل

**كنتَ محقاً. لم أفعلها.**

---

## ما شحنتُه فعلاً وسمّيتُه «إصلاحاً»

```js
const PALETTE = ['#2563EB','#7C3AED','#DB2777', …];
x.fillStyle = grad;  x.fillRect(0, 0, 240, 180);
x.fillText('Réviser', 120, 92);
url = c.toDataURL('image/png');
```

**مستطيلات ملوّنة بكلمة مكتوبة عليها.** «Réviser» على صندوق أزرق،
«Café» على صندوق برتقالي. و`fetch()` تظهر **صفر مرة** في الملف.

والأسوأ أنني كتبتُ تعليقاً يصفها بـ«placeholder tiles» ثم أبلغتُ أن
خطأ الـGIF **أُصلح**. أصلحتُ الشيء الخطأ: جعلتُ الصناديق الفارغة
تُعرَض، بدل أن أجعلها GIF فعلية.

وكان هناك `useGifProvider()` — **وصلة لا يستدعيها أحد**. للمرة
الثالثة في هذا المشروع: نفس نمط قاعدة البيانات، ونفس نمط اللعبة.

---

## ما صار الآن

### مستويان، والأول يعمل بلا مفتاح

```
مع مفتاح Tenor   →  بحث حيّ في الكتالوج كاملاً (نفس ما تستخدمه
                     إنستغرام وواتساب)
بلا مفتاح        →  مكتبة GIF حقيقية متحرّكة من CDN جيفي
```

اخترتَ «بلا مفتاح»، فهذا ما تحصل عليه اليوم — **وTenor مكتوبة
وجاهزة**، تحتاج تغيير سطر واحد في `config_sm.js` حين تريد.

### كل رابط تحقّقتُ منه بنفسي

لم أكتب معرّفاً واحداً من الذاكرة. كل واحد جُلب وفُحصت بايتاته:

```
unique GIF ids shipped: 16
  full GIFs live:  16 / 16      ← HTTP 206 + MAGIC=GIF89a
  previews live:   16 / 16
```

`GIF89a` هي بصمة ملف GIF متحرّك. لو كان أحدها صورة ثابتة أو صفحة
خطأ، لظهر هنا.

### وتفصيل يهمّ على شبكة الجامعة

```js
const CDN   = id => `…/${id}/giphy.gif`;   // الكامل، عند الإرسال
const THUMB = id => `…/${id}/200w.gif`;    // المصغّر، في الشبكة
```

شبكة من ٢٠ صورة كاملة = عشرات الميغابايت على اتصال جامعي. المصغّرات
تُحمَّل في الشبكة، والكامل يُرسَل فقط عند الاختيار.

---

## ما أضفتُه أثناء ذلك

```
بحث حيّ            بتأخير ٣٠٠ms، يبحث في المكتبة أو في Tenor
هياكل عظمية        تحجز ارتفاع الشبكة فلا تقفز تحت المؤشّر
إسناد المصدر       Tenor وجيفي يشترطانه، ويخبرك أي مصدر تتصفّح
نصّ بديل            لكل GIF، لقارئ الشاشة
حركة مخفّضة        `prefers-reduced-motion` يوقف الشبكة المتحرّكة
محتوى آمن          `contentfilter: 'high'` — تطبيق جامعي
```

وخطأ صغير كشفه الاختبار: أيقونة `like` غير موجودة في المجموعة،
فكانت الفئة بلا أيقونة. صارت `fire`.

---

## الاختبارات

```
770/770
```

و**١٠ حوارس دائمة** تمنع تكرار ما فعلتُه:

```js
ok('no canvas-drawn fake GIFs',  !/createElement\('canvas'\)/.test(gifSrc));
ok('no colour palette',          !/const PALETTE/.test(gifSrc));
ok('a real GIF library',         new Set(gifIds).size >= 12);
ok('urls point at a real CDN',   /media\.giphy\.com/.test(gifSrc));
ok('provider seam has a real default, not a dead hook', …);
```

الأخير هو الأهم: يفحص أن الوصلة **موصولة**، لا مجرد موجودة.

**واختباران قديمان** كانا يؤكّدان أن البطاقات تُرسَم على canvas —
كانا يكرّسون الإصلاح الخطأ. قلبتُهما.

---

## لتفعيل Tenor لاحقاً

```
١. console.cloud.google.com → فعّل Tenor API → أنشئ مفتاحاً
٢. config_sm.js:  GIF_KEY: 'المفتاح'
```

وينتقل التطبيق تلقائياً من ١٦ صورة إلى الكتالوج كاملاً. **وإن فشل
Tenor** يعود إلى المكتبة بدل أن يعرض شبكة فارغة.

---

## ما لم أتحقّق منه

**لم أفتح متصفحاً**، فلم أرَ الصور تتحرّك بعيني. تحقّقتُ من أن
الروابط تعيد بايتات GIF متحرّكة، وأن الشبكة تعرض ١٦ بطاقة تشير إليها.

**والأهم:** جيفي CDN قد يُحجب في بعض الشبكات الجزائرية. إن لم تظهر
عندك، أخبرني — الحل حينها إمّا Tenor بمفتاح أو استضافة الصور في
قاعدتك مباشرة كما فعلنا مع صور البروفايل.
