# اللغات — الآن تعمل فعلاً

**كنتَ محقاً، والقياس يثبته.**

---

## ما كان الوضع حين قلتَ «are u playing w me»

```
الوحدات التي تستورد t():   3 من 28
نصوص فرنسية مكتوبة يدوياً:  ~358
```

شغّلتُ التطبيق بالإنجليزية ومررتُ على الشاشات كطالب:

```
feed           تسريب فرنسي: faculté, jour
messages       تسريب فرنسي: Aucun, Fichier, Supprimer, Bloquer, Signaler
notifications  تسريب فرنسي: votre
hub            تسريب فرنسي: Niveau, jour, Défis, Répondre, Publier
events         تسريب فرنسي: Créer, jour
qa             تسريب فرنسي: Question, Réponse, Meilleure
profile        تسريب فرنسي: Modifier, Publication
الشريط الجانبي  "Accueil Explorer Messages Notifications Hub Canaux…"
```

**٨ من ٩ شاشات، والشريط الجانبي فرنسي بالكامل.**

بنيتُ محرّك ترجمة ولم أوصل به شيئاً — **نفس خطأ «الوصلة التي لا
يستدعيها أحد»** الذي ارتكبتُه مع قاعدة البيانات، مكرّراً حرفياً.

---

## الآن

```
feed           نظيف        events    نظيف
messages       نظيف        qa        نظيف
notifications  نظيف        channels  نظيف
hub            نظيف        explore   نظيف
profile        نظيف        settings  نظيف

  صفر عنصر واجهة غير مترجَم
```

```
[en] dir=ltr  Home Explore Messages Notifications Hub Channels…
[fr] dir=ltr  Accueil Explorer Messages Notifications Hub Canaux…
[ar] dir=rtl  الرئيسية استكشاف الرسائل الإشعارات المركز…
```

---

## ما تغيّر فعلياً

**٣٠٦ مفاتيح × ٣ لغات**، تطابق تام. و**١٧٥ نصاً** حُوِّل إلى `t()`
عبر ٩ وحدات.

### ١. عناوين المسارات صارت مفاتيح

```js
feed: { title: 'nav.home' }     // بدل 'Accueil'
```

فأي مسار يُضاف لاحقاً **لا يستطيع أن ينسى الترجمة**.

### ٢. الشاشة المفتوحة تُعاد رسمها

هذا هو الفرق بين «الترجمة موجودة» و«اللغة تعمل»:

```js
on(window, 'koliya:i18n', () => render({ ...current }));
```

بدونه تترجم النصوص، لكن الشاشة التي أمامك تبقى بلغتها القديمة حتى
تنتقل لصفحة أخرى. **وهذا بالضبط ما كنتَ تراه.**

### ٣. التواريخ تتبع اللغة

كانت `timeAgo(ts, locale = 'fr')` وكل مستدعٍ يحذف الوسيط:

```js
export const activeLocale = () => document.documentElement.lang || 'en';
export function timeAgo(ts, locale = activeLocale()) { … }
```

`1h` · `1 h` · `١ س` — كلٌّ بلغته.

### ٤. التحدّيات والشارات

كانت التسميات نصوصاً مخزّنة، فتبقى فرنسية بعد التبديل:

```js
{ id:'post', key:'quest.post' }        // مفتاح، يُترجَم عند الرسم
export const questLabel = q => t(q.key);
```

---

## الاختبار الذي كان يجب أن يوجد من البداية

`tests/language.test.mjs` — **٣٨ اختباراً** تقود الواجهة الحقيقية
وتقرأ النص المرسوم، لا `t()` وحدها.

الاختبار القديم كان يفحص أن `t('nav.home') === 'Home'` — وينجح بينما
٨ شاشات فرنسية، لأن لا أحد كان يستدعي `t()`.

والجديد فيه **حارس انحدار**:

```js
for (const mod of MUST_TRANSLATE)
  ok(`${mod} uses the translation system`,
     /i18n_sm\.js/.test(src) && (src.match(/\bt\(/g)||[]).length >= 3);
```

فإن توقّفت وحدة عن استخدام الترجمة، يفشل الاختبار فوراً.

---

## وتسعة اختبارات كانت هشّة

كانت تفحص كلمات فرنسية، فانكسرت حين صارت الإنجليزية افتراضية.
لم أخفِّف الفحص — **جعلتُه يفحص السلوك**:

```js
// قبل: نصّ فرنسي
const anonCard = [...cards].find(c => c.textContent.includes('Anonyme'));

// بعد: البيانات نفسها — وهذا يحرس وعد الخصوصية
const anonQ = st.questions.find(q => q.anonymous);
ok('anonymous carries no author id', anonQ.user_id === null);
```

اختبار الخصوصية صار **أقوى** من قبل: لم يعد يعتمد على كلمة قد تتغيّر.

---

## المجموع

```
674/674   (كان 635، والجديد +38 لغة +1 حارس)
```

---

## ما لم أفعله

**لم أفتح متصفحاً حقيقياً.** jsdom يرسم DOM لا بكسلات، فاتجاه RTL
مُختبَر عبر `dir="rtl"` والقواعد المنطقية، لا بالشكل النهائي.

**جرّب هذا:**

```
بدّل إلى العربية وأنت في الـHub  → هل تتغيّر فوراً بلا انتقال؟
افتح محادثة بالعربية             → هل الفقاعات تبدأ من اليمين؟
انظر إلى وقت رسالة               → «١ س» أم «1h»؟
منشور كتبه أحدهم بالفرنسية       → يبقى فرنسياً (وهذا صحيح)
```

النقطة الأخيرة مقصودة: **الواجهة تُترجَم، ومحتوى الطلبة لا.**
