# V11 — الرسائل تصل من أي صفحة، والأزرار تعمل فعلاً

**`apk/Koliya-1.00.apk` · versionCode 5 · 3.8 MB · موقّع · zipaligned**

---

## كنت أُصلح النصف الخطأ

في المرات الثلاث السابقة بنيت خدمة خلفية في جافا وأصلحت مُشغّل قاعدة
البيانات. كلاهما صحيح — لكن العطل الذي تصفه كان في **جافاسكريبت**، وموجود
منذ البداية. هذه أعطال قِستها في الكود لا خمّنتها.

---

## العطل 1 — الاستطلاع لا يعمل إلا وأنت داخل محادثة

`messages_sm.js` أول سطر في دالة الاستطلاع:

```js
async function beat(force = false) {
  if (pollBusy || !peer || !api?.listMessages) return;   // ← هنا
```

`peer` هو الشخص الذي تحادثه الآن. بلا محادثة مفتوحة يكون `null`
و **الدالة تخرج دون أن تسأل الخادم شيئاً**. وأسوأ: `startPolling()` تُستدعى
من داخل `openThread()` فقط، و`teardownMessages()` توقفها عند مغادرة الصفحة.

| أين أنت | هل يستطلع؟ |
|---|---|
| داخل المحادثة | ✅ كل 1.5 ث |
| قائمة الرسائل، بلا محادثة | ❌ `peer` فارغ |
| الرئيسية / أي صفحة | ❌ المؤقّت متوقّف |

هذا نصّ شكواك: «يجب أن أكون داخل صفحة الرسائل، وداخل المحادثة».

### الحل — `core/inbox_sm.js`
مستطلِع **غير مرتبط بأي صفحة**: يبدأ عند تسجيل الدخول ويعمل على كل
المسارات. كل 5 ث والتطبيق مرئي، 20 ث وهو مخفي، وفوراً عند العودة إليه.
يحدّث الشارة، ويعيد رسم قائمة المحادثات، ويطلق `dm:incoming`.

استطلاع المحادثة المفتوحة (1.5 ث) بقي كما هو للحوار الحيّ.

**+ لا وميض**: `renderConvList()` كانت تمسح القائمة إلى هياكل تحميل في كل
نداء — كان سيومض كل 5 ثوانٍ. أضفت وضعاً هادئاً للتحديث الخلفي.

---

## العطل 2 — الشارة لا تتحدّث خارج صفحة الرسائل

`setState({ unread… })` كانت تُكتب **داخل `renderConvList()` فقط**. فخارج
صفحة الرسائل تبقى الشارة على آخر رقم رأيته. الآن `shell_sm.js` يستمع إلى
`inbox:unread` من المستطلِع.

---

## العطل 3 — الإشعار لا يظهر إلا إذا كانت الأذونات ممنوحة

```js
onEvent('dm:incoming', ({...}) => {
  if (!canNotify() || muted) return;   // ← بلا إذن النظام: لا شيء إطلاقاً
```

بلا إذن إشعارات النظام **لم يكن يحدث أي شيء**: لا لافتة، لا صوت، لا علامة
— حتى والتطبيق مفتوح أمامك. وكان يُطلق من `beat()` أي من نفس الدالة
المعطّلة.

### الحل — قواعد إنستغرام بالضبط
| الحالة | السلوك |
|---|---|
| داخل نفس المحادثة | **صمت** — أنت تراها |
| في التطبيق، صفحة أخرى | **لافتة داخلية** تضغطها فتفتح المحادثة |
| التطبيق مخفي/مغلق | إشعار نظام |

اللافتة تعمل **بلا إذن النظام**. وطبّقتها أيضاً على المتابعة والإعجاب
والتعليق والإشارة، لا الرسائل وحدها.

**+ اللافتة كانت تغطّي الشريط السفلي** — قِستها: تخفي شارة الرسائل التي
تعلن عنها. الآن ترتفع فوق الشريط (قِست: أسفلها 845، أعلى الشريط 857).

---

## العطل 4 — كل أزرار المنشور ميتة في الصفحة الرئيسية

```js
const p = posts.find(x => x.id === card.dataset.id);
```

`posts.id` نوعه `bigserial` ← نيون يرسل **رقماً**. و`dataset.id` **نصّ**
دائماً. فـ `1 === "1"` تساوي `false` ← `p` غير معرّفة ← **كل معالج يخرج
صامتاً**: الإعجاب، التعليق، إعادة النشر، المشاركة، الحفظ، القائمة،
التصويت، والنقر المزدوج.

وقلتَ إنها تعمل في صفحة الملف الشخصي — **صحيح**، لأن `profile_sm.js`
يستعمل `String(x.id) === String(...)`. الاختبارات لم تكشفه لأن الخادم
الوهمي يستعمل معرّفات نصّية (`'p1'`).

أُصلح في 4 مواضع بدالة `findPost()` واحدة.

### والأرقام كانت تختفي عند الصفر
```js
<span class="c">${p.likes.length || ''}</span>
```
`0 || ''` = فراغ. منشور بلا إعجاب لا يعرض `0` بل **لا شيء** — فتضغط ولا
ترى رقماً يتغيّر، فتبدو الأزرار «ثابتة». الآن `compact()` تعرض `0` دائماً
و`1.2K` للكبيرة، **وأضفت عدّاداً لزرّ المشاركة** (لم يكن له عدّاد أصلاً).

---

## الأرقام
```
tests/run.sh           852/852   jsdom
tests/browser/run.sh   267/267   Chrome حقيقي  (inbox 22 جديدة)
build_apk.sh            22/22    فحص محتوى داخل الملف المُسلَّم
```

### الاختبار الجديد يُثبت نفسه
اختبار يمرّ بلا معنى ما لم يكن ليفشل قبل الإصلاح. عطّلت المستطلِع مؤقتاً:
```
19/22 — FAIL الشارة ظهرت بلا فتح الرسائل
        FAIL الشارة تعرض رقماً حقيقياً
        FAIL القائمة أعادت الرسم بنفسها
```
ثم أعدته: **22/22**. الاختبار يمسك العطل فعلاً.

### قياس من الملف المُسلَّم (لا من `public/`)
```
M2: badge 4 → 5 والرسالة وصلت وأنا على الصفحة الرئيسية
    counts: like 2 · comment 2 · repost 0 · share 0   (كلها ظاهرة)
```

---

## ما لم أفعله بعد — من ردّك الأخير
ذكرت ثلاثة أشياء لم أصلها في هذه الجولة، وأفضّل أن أقولها بدل أن أدّعي:
1. **زرّ إنشاء منشور (+) في الهاتف** — غير موجود؛ `rail-compose` مخفي في
   `layout_sm.css:652`. تحقّقت لكن لم أبنِه.
2. **Explore للبحث فقط** — يبحث في الأشخاص والمنشورات فعلاً، لكن به قسم
   اكتشاف أيضاً. لم ألمسه.
3. **الوصول الفوري والتطبيق مغلق** لكل إشعار — يحتاج FCM؛ الحالي استطلاع.

## ⚠️ خطوة إلزامية
**شغّل `db/FULL_SCHEMA_sm.sql` في Neon** ← Data API ← Refresh schema cache.
مُشغّل V10 (`trg_notify_new_message`) هو ما يكتب صف الإشعار أصلاً.

## ما لم أقِسه
**لم أشغّل الـ APK ولا مرة** — لا `/dev/kvm`، المحاكي يرفض الإقلاع.
الفحص بنيوي وسلوكي في Chrome فقط، لا على هاتف.
