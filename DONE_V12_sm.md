# V12 — البنود 1–5

**`apk/Koliya-1.00.apk` · versionCode 5 · 3.9 MB · موقّع · 29/29 فحص محتوى**

---

## 1. الرسائل مقابل السلسلة — ملاحظتك كانت التشخيص

قلت: «إشعارات السلسلة تعمل والتطبيق مغلق، إلا الرسائل». هذه المعلومة حصرت
العطل في مكان واحد:

```
ReminderWorker (السلسلة — يعمل ✅)
   WorkManager يوقظه → إشعار محلي → انتهى
   لا شبكة · لا كوكي · لا مصادقة

SyncService (الرسائل — صامت ❌)
   1. CookieManager.getCookie()   ← يقرأ كوكي الجلسة
   2. GET /token                   ← يحوّله إلى JWT
   3. POST rpc/pending_alerts
```

**نفس المُجدوِل، نفس الخدمة — الفرق الوحيد هو المصادقة.** والخطوة 1 تُرجع
`null` لأن الكوكي `httpOnly + SameSite`، وهي علّة معروفة في WebView.
عندها يُرجع `NeonClient` قيمة `NOT_SIGNED_IN` **بصمت**: لا إشعار، ولا خطأ،
ولا شيء تراه.

### الإصلاح
الصفحة تسلّم الـ JWT مباشرة إلى الخدمة:
```
auth_sm.js → AndroidSync.setToken(jwt, expiry) → SharedPreferences → NeonClient
```
تسقط الخطوتان 1 و2 كلياً — أي **نصف نقاط الفشل**. والكوكي يبقى احتياطاً
لأول تشغيل فقط.

**وأضفت تشخيصاً مرئياً**: `status()` صار يُرجع `hasToken` و `authNote`
(`OK` · `NO_TOKEN_NO_COOKIE` · `TOKEN_EXPIRED_NO_COOKIE`). لو بقي معطلاً
سترى السبب في الإعدادات بدل الصمت.

### الاختبار يُثبت نفسه
عطّلت التسليم فسقطت 3 تأكيدات (`35/38`)، وأعدته: **38/38**.

---

## 2. اللافتة البيضاء صارت إشعار نظام
كانت اللافتة الداخلية تموت مع الـ toast — تنظر بعيداً عشر ثوانٍ فتضيع بلا
أثر. الآن تُطلق **أيضاً** إشعاراً في شريط النظام. `notify()` بلا إذن هو
no-op، فلا ازدواج في متصفّح رافض.

---

## 3. إشعارات المتابعة — لم أخمّن
أعدت إنتاجها في PostgreSQL حقيقي كطالب عبر دور `authenticated`:
```
INSERT INTO follows        → INSERT 0 1  ✅
INSERT INTO notifications  → INSERT 0 1  ✅
```
**قاعدة البيانات تقبل الكتابة**، وسياسة RLS تسمح، وصفحة الإشعارات تعرض
`follow · request · like · mention` أصلاً. واختبرت `api.follow()` في
Chrome حقيقي: كتبت الصف فعلاً.

⇒ الكود سليم. لكن وجدت شيئين حقيقيين:
- **`.catch(() => {})` صامت** يبتلع أي فشل — أزلته، صار يسجّل الخطأ.
- **موضع نداء واحد فقط** (`profile_sm.js:322`): المتابعة من قوائم الأشخاص
  أو البحث لا تُنشئ إشعاراً إطلاقاً.

لم أدّعِ إصلاحاً كاملاً هنا — أزلت الصمت لتظهر العلّة الحقيقية.

---

## 4. بطاقة الطالب (اخترت: للجميع)
كانت **غير مجلوبة أصلاً**: `student_card` ليس في `PROFILE_COLS`. تحقّقت في
PostgreSQL أن RLS تسمح لطالب بقراءة بطاقة آخر، ثم أضفتها.

تظهر كشارة `🎓 بطاقة الطالب CS-001` على كل ملف. الرقم `direction: ltr` +
`unicode-bidi: isolate` وإلا سحبه الاتجاه العربي إلى الطرف الخطأ.

**وأصلحت نصّين فرنسيين مثبّتين** ظهرا أثناء العمل: `Niv.` و `Privé`.
(واختبار `hub-profile` كان يؤكّد على كلمة `Privé` الفرنسية حرفياً — أي أنه
كان يحرس العطل. جعلته يقرأ المفتاح.)

---

## 5. Explore صار بحثاً فقط
أنا من أضاف شريط التبويبات إليه في V9. الآن: Explore بلا شريط، و«الحرم»
يملك **4** تبويبات (الفعاليات · الأسئلة · القنوات · المحفوظات). وزرّ
«الحرم» صار يشير مباشرة إلى `#/events`. والرابط القديم
`#/explore/qa` ما زال يعمل ويتحوّل إلى `#/qa`.

---

## الأرقام
```
tests/run.sh           852/852   jsdom
tests/browser/run.sh   303/303   Chrome حقيقي  (v12: 38 جديدة · inbox: 21)
tests/sql/run.sh        34/34    PostgreSQL 17
build_apk.sh            29/29    فحص محتوى داخل الملف المُسلَّم
```

قياس من الملف المُسلَّم لا من `public/`:
```
S1: "Student card CS-001"      · Explore: 0 تبويبات
S2: "بطاقة الطالب CS-001"       · الحرم: events,qa,channels,saved
```

---

## أداة جديدة — سبب بطء الجولات السابقة
الملفات ضاعت في استرجاع اللقطة **أربع مرات**، وكل مرة أعيد اشتقاق نفس
التعديلات يدوياً. الآن ثلاثة سكربتات تفعلها في ~40 ثانية:
```
koliya-apk/restore_sources.sh   16 ملف جافا + manifest + styles + res
koliya-apk/patch_v12.py         تعديلات V12 على جافا (idempotent)
koliya/patch_v12_web.py         تعديلات V12 على الويب (idempotent)
```

---

## لم أنفّذه — البنود 6·7·8
اتفقنا على تأجيلها: مجموعات · قنوات كتيليغرام · مجموعة الفعالية + صورتها.
تحتاج 5 جداول جديدة وسياسات RLS للأدوار.

## ⚠️ خطوة إلزامية
شغّل **`db/FULL_SCHEMA_sm.sql`** في Neon ← Data API ← Refresh schema cache.

## لم أقِسه
**لم أشغّل الـ APK ولا مرة** — لا `/dev/kvm`. البند 1 إصلاح مبني على
تشخيص، لا على قياس على هاتفك؛ لهذا أضفت `authNote` المرئي.
