# V13 — تعديلات موضعية، بلا إعادة بناء

سمعتك: لا إعادة كتابة. عدّلت الملفات القائمة فقط.

---

## 1. «الإعجاب بمنشوري يظهر على كل الحسابات»

هذا كان **أخطرها**، والسبب سطر واحد في `store_sm.js`:

```js
export function scoped(namespace) {
  const p = namespace + ':';     // ← لا يوجد أي معرّف مستخدم
```

كل حساب على نفس الجهاز كان يكتب في **نفس المفاتيح**: علامات الإعجاب،
القصص المشاهَدة، تقدّم التحديات، تفضيلات المحادثات، وعلامة الإشعارات.
فما يكتبه حساب يقرأه الحساب التالي.

**الإصلاح**: المفتاح صار `namespace:<userId>:key`. والمعرّف يُقرأ **وقت
النداء** لا وقت التحميل — لأن كل وحدة تنادي `scoped()` عند الاستيراد، أي
قبل تسجيل الدخول بكثير؛ لو التقطته مرة واحدة لتجمّد كل شيء على `anon`
(نفس فخّ «التجمّد وقت الاستيراد» الذي جمّد اللغة في 13 موضعاً).

**أثبتّه بالقياس** لا بالادعاء:
```
1 anon  : "anon-value"
2 alice : null          ← لا ترى ما كتبه غيرها
3 bob   : null          ← ولا يرى ما كتبته أليس
4 alice : "alice-value" ← وقيمتها سليمة بعد كتابة بوب
المفاتيح: kl:probe:acct_alice:x · kl:probe:acct_bob:x
```

---

## 2. الحساب الخاص — كل ما حوله كان غير دقيق

كانت المنشورات محجوبة **فقط**. أما المتابِعون والمتابَعون فكانت أرقامهم
قابلة للنقر و**تعرض كل الأسماء لأي شخص**.

الآن:
- العددان **مقفلان** (`disabled` + بلا مؤشّر) لغير المتابِع
- الضغط لا يفتح شيئاً — **قِست: 0 أسماء**
- **الرقم يبقى ظاهراً** (إنستغرام يفعل الشيء نفسه: يعرض «142 متابع» ويخفي
  القائمة) — إخفاء الرقم كذب، إخفاء القائمة خصوصية
- المنشورات وبقية الصفحة كما هي

وبوابة واحدة `graphHidden()` تحكم الثلاثة، فلا تتفرّق القواعد مجدداً.

---

## 3. زرّ «إزالة متابِع»
`removeFollower()` جديدة تحذف **متابعته لي** (غير إلغاء متابعتي له) — وهي
الطريقة الوحيدة لإخراج أحدهم من حساب خاص بعد قبوله. تحقّقت أن RLS تسمح
(`follows_delete` تغطّي `followee_id = auth.user_id()`).

يظهر **في قائمة متابِعيّ أنا فقط** — لا في قائمة غيري ولا في «يتابعهم».
مع تأكيد قبل الحذف، وتحديث فوري للعدّاد.
**قِست: الصف يُحذف من الخادم فعلاً (7 → 6).**

قبول/رفض طلبات المتابعة كان موجوداً ويعمل (`respondToRequest`).

---

## 4. صفحة الإشعارات لا تُظهر الأرقام
وجدت **سببين**، لا واحداً:

**أ)** `refreshNotificationBadge()` كانت تُنادى **مرة واحدة عند الإقلاع**
فقط. أي إعجاب أو متابعة بعدها لا تغيّر شيئاً. الآن مربوطة بـ
`notify:alerts` فتتحدّث حيّاً — والقائمة تُعاد رسمها إن كانت معروضة.

**ب) الأخطر**: `checkAlerts()` كانت تبدأ بـ
```js
if (!canNotify() || !me.id) return;
```
أي أن **خطّ الإشعارات كله ميت** بلا إذن نظام: لا عدّاد، ولا لافتة داخلية،
ولا تحديث قائمة. من ضغط «ليس الآن» مرة — وكل WebView لأن الإذن فيه يبقى
`default` أبداً — كان يرى الجرس مجمّداً. الآن الاستطلاع يعمل دائماً،
والإذن يحكم **إشعار الشريط وحده**.

**قِست: الجرس 2 → 3 وأنا على الصفحة الرئيسية، بلا إعادة تحميل.**

---

## أخطاء في أدوات الاختبار كانت تخفي أعطالاً حقيقية
- **`pending_alerts` في الخادم الوهمي كانت `return S(200, [])`** — كعب
  ثابت. أي أن خطّ الإشعارات بأكمله **لم يكن مُختبَراً إطلاقاً**. نفّذتها
  فعلياً مطابقةً للـ SQL.
- **جدول `follows` في البيانات التجريبية بلا عمود `state`** — والاستعلام
  يرشّح `state=eq.accepted`، فكانت `followers()` تُرجع صفراً دائماً بينما
  تعمل في الإنتاج. أضفته لـ 11 صفاً.
- 3 اختبارات كانت تقرأ المفتاح الخام `board:scope` — حوّلتها إلى
  `scoped('board').get('scope')` فلا تنكسر مع أي تغيير في البادئة.

## وأشياء صغيرة صحّحتها في الطريق
`et`/`autres` الفرنسيتان في تجميع الإشعارات · عنوان النافذة كان يعرض
`followers` بحرف صغير (كانت تسمية إحصائية «7 followers» مُعاد استعمالها
كعنوان) · نصّ «تابع فلاناً لرؤية منشوراته» كان فرنسياً مثبّتاً.

---

## الأرقام
```
tests/run.sh           852/852   jsdom
tests/browser/run.sh   274/274   Chrome حقيقي  (v13: 30 جديدة)
```
الصور: `V1-private` · `V2-remove-follower` — نظرت إليهما بنفسي.

## لم أبنِ APK
تغييرات هذه الجولة كلها ويب. الـ APK الحالي `Koliya-1.00.apk` لا يحوي
هذه الإصلاحات — قل لي متى تريد بناءه وأبنيه (البناء وحده ~10 دقائق).

## أدوات تمنع تكرار البطء
```
koliya/patch_v12_web.py      إصلاحات V12  (idempotent)
koliya/patch_v13_web.py      إصلاحات V13  (idempotent)
koliya-apk/restore_sources.sh + patch_v12.py
```
الملفات رجعت للخلف 5 مرات في هذه الجلسة؛ هذه السكربتات تعيدها في ثوانٍ
بدل ساعة من إعادة الاشتقاق اليدوي.

## ما لم أفعله
البنود المؤجّلة كما هي: المجموعات · القنوات كتيليغرام · مجموعة الفعالية
وصورتها · زرّ **+** لإنشاء منشور في الهاتف.
