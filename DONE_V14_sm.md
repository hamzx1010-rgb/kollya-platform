# V14 — زرّ +، محادثات المجموعات والقنوات والفعاليات

**`apk/Koliya-1.00.apk` · 3.9 MB · 37/37 فحص محتوى داخل الملف المُسلَّم**

---

## أولاً: لماذا لم تر إصلاحات المتابعين

**لأنني لم أبنِ الـ APK بعد V13.** فحصت الملف الذي كان بيدك:
```
graphHidden داخل APK : 0
```
الكود كان في `public/` والصور كانت حقيقية — لكن الملف على هاتفك أقدم منها.
هذه المرة بُني، والفحص يؤكّد وجودها:
```
ok  private account gate   js/features/profile_sm.js (4)
ok  remove follower        js/core/api_sm.js (1)
ok  per-account storage    js/core/store_sm.js (2)
```

---

## 1. زرّ + في الأعلى (الأهم)
`rail-compose` كان `display:none` تحت 900px — **فلم تكن هناك أي طريقة
لنشر منشور من الهاتف إطلاقاً**. أضفت زرّاً في الشريط العلوي كإنستغرام.

قِست: ظاهر على 412px، يفتح المُنشئ بمربّع نصّ، **ومخفي على الحاسوب**
(الشريط الجانبي يملك واحداً؛ عرض الإجراء مرتين يفتح مُنشئَين).

## 2. محادثات القنوات — بنظام تيليغرام
`db/14_groups_sm.sql`: أدوار (مالك/مشرف/عضو) · قناة خاصة · «المشرفون فقط»
· طلبات انضمام · جدول `group_messages`.

**قِست في PostgreSQL حقيقي كطالب عبر دور `authenticated`:**
```
عضو ينشر في قناة «المشرفون فقط»  → RLS ترفض · 0 صفوف
مشرف ينشر في نفسها                → 1 ✅
غريب يقرأ قناة ليس فيها           → 0 صفوف
الانضمام لقناة خاصة               → requested · 0 أعضاء · 1 طلب
عضو يرقّي نفسه                    → f (مرفوض)
المالك يرقّي                       → t → admin
```
**القرار في قاعدة البيانات لا في المتصفّح** — تعديل الصفحة لا يتجاوزه.

وفي الواجهة: عضو في قناة «مشرفون فقط» **لا يرى مربّع كتابة أصلاً** بل
سطراً يشرح السبب. عرض مربّع سترفضه قاعدة البيانات أسوأ من عدم عرضه.

**خطأ وجدته أثناء الاختبار**: القناة المُنشأة *بعد* الترحيل لم يكن لها صف
مالك، فلا يستطيع مُنشئها إدارتها. أضفت مُشغّلاً — اكتشفته لأن
`channel_role()` رجعت فارغة على قناة جديدة.

## 3. محادثة الفعالية
الحضور **هو** العضوية: مُشغّل على `event_attendees`. لا رتب في محادثة
الفعالية — كل من يحضر متساوٍ.
قِست: `is_event_attendee` صحيحة، وغير الحاضر **تُرفض كتابته**.

## 4. صورة الفعالية + جانبها
مُنتقي صورة في نافذة الإنشاء، واختيار يمين/يسار. الصورة تظهر **بجانب**
المعلومات على الشاشات العريضة وفوقها على الهاتف.
في العربية تنعكس الجهة تلقائياً، فأضفت قاعدة تُلغي الانعكاس — وإلا صار
اختيار المنظّم يعني عكس ما رآه.

---

## أخطاء أمسكتها الاختبارات أثناء العمل
- `--r-md` غير معرّف (اخترعته) — اختبار CSS أمسكه
- `channels.members` **مكرّر** — كان موجوداً كعدّاد صغير؛ المفتاح المكرّر
  يدهس الأول بصمت
- `action.accept` / `action.decline` **غير معرّفين** — ظهرا نصّاً خاماً في
  الصورة؛ `dm.accept` موجود أصلاً بثلاث لغات
- `wireComposer()` كانت تنهار على `peer.id` في محادثة جماعية (لا يوجد
  «طرف» واحد) — فبقي زرّ الإرسال معطّلاً ولم يكن الإرسال ممكناً إطلاقاً
- دوال المجموعات كانت على `campusApi` بينما الشاشة تحمل `messagesApi`
- `/^(event|channel)-(\d+)$/` كان يرفض المعرّفات غير الرقمية

---

## الأرقام
```
tests/run.sh              852/852   jsdom
tests/browser/groups.mjs   47/47    Chrome حقيقي (جديد)
PostgreSQL 17                       كل قيود الأدوار مُتحقَّقة
build_apk.sh               37/37    فحص محتوى داخل الملف المُسلَّم
```
اختبرت **الجديد فقط** كما طلبت، عدا `tests/run.sh` لأنه سريع (60 ث) ويمسك
المفاتيح المكرّرة والمتغيّرات غير المعرّفة.

الصور: `W1-plus-button` · `X1-group-chat` · `X2-channel-admin` · `X3-readonly`

## ⚠️ إلزامي
**شغّل `db/FULL_SCHEMA_sm.sql` من جديد** (2165 سطراً الآن) ← Data API ←
Refresh schema cache. بدونه لا توجد جداول المجموعات والقنوات لن تعمل.

## لم أقِسه
لم أشغّل الـ APK ولا مرة — لا `/dev/kvm`. الفحص بنيوي + Chrome حقيقي.
