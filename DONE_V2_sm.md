# نُفِّذت خطة V2 — ما تغيّر بالضبط

**542/542 اختبار ناجح** · **7 ملفات SQL تُحلَّل بلا خطأ** · **26 وحدة تُحمَّل**

---

## ١. `safeUrl()` — سطر واحد أعاد 44 موضعاً

```js
// قبل — data: محجوبة، فكل صورة فارغة
return /^(https?:|blob:|\/)/i.test(s) ? s : '';

// بعد — data: مسموحة للصور والفيديو والصوت،
//        وممنوعة لكل ما يمكن أن يُنفَّذ
const SAFE_DATA = /^data:(image\/(png|jpe?g|gif|webp|avif|bmp)
                        |video\/(mp4|webm|ogg)
                        |audio\/(mpeg|mp3|ogg|wav|webm|mp4|aac))[;,]/i;
```

**SVG مستثناة عمداً** رغم أنها صورة: ملف SVG يمكن أن يحمل `<script>`،
فتصبح «الصورة الشخصية» كوداً ينفّذ.

**تحقّقت فعلاً، لا بالتخمين:**

```
FEED:
  post images                 1 imgs, 0 blank
  avatars                     4 imgs, 0 blank
  story ring avatars          4 imgs, 0 blank
PROFILE:
  profile avatar              1 imgs, 0 blank
  banner (css background)     present

  every data: image rendered with a real src
```

---

## ٢. زر الحذف خلف الستوري — والمشكلة كانت أوسع

بحثتُ عن النظائر كما طلبت. القديم:

```
.sv        عارض الستوري   500  ← الغطاء
.toast                    400  مدفون
.menu                     300  مدفون
.modal                    201  مدفون
.lightbox                 500  تعادل → DOM يقرّر عشوائياً
```

الجديد — **السطح الغامر تحت كل ما يمكنه فتحه**:

```
800  toast (التأكيدات)
700  menu  (القوائم السياقية)
601  modal (تأكيد الحذف · قائمة المشاهدين)
600  scrim
460  lightbox
450  .sv   (عارض الستوري)
100  الشريط الجانبي
```

`tests/layers.test.mjs` يقرأ CSS الحقيقي ويتحقّق رقمياً — 22 اختباراً،
منها سلسلة العطل بالضبط: *افتح ستوري → احذف → هل التأكيد مرئي؟*

---

## ٣. العدّاد الذي لا يستطيع العدّ

`db.count()` كانت تفرض `select=id`، لكن جداول الوصل بلا `id`:

```
follows          PRIMARY KEY (follower_id, followee_id)
post_likes       PRIMARY KEY (post_id, user_id)
post_saves · event_attendees · channel_members · story_views …
```

كل استعلام يعود 400، و`.catch(() => 0)` يبتلعه. أضفتُ `COUNT_KEY`
يربط كل جدول بعموده:

```
  OK  follows           counts on 'follower_id'   (no id column)
  OK  post_likes        counts on 'post_id'       (no id column)
  OK  event_attendees   counts on 'event_id'      (no id column)
  … كل الاستعلامات تشير إلى عمود موجود
```

**والأهم:** عدّادات البروفايل صارت عبر `profile_counts()` —
دالة `SECURITY DEFINER`، فالرقم صحيح حتى على حساب خاص تخفي RLS صفوفه.
(إنستغرام يعرض «142 متابعاً» على حساب مقفل ويخفي القائمة — هذا هو.)

---

## ٤. الجرس: شكلان متصادمان

```js
setState({ unread: { …, notifications: n } });   // كائن، في وحدة
setState({ unread: n });                          // رقم، في أخرى
```

`shell_sm.js` يقرأ `unread.messages + unread.notifications` → **NaN**.
وحّدتُ الشكل، **وأضفتُ نصف الرسائل الذي لم يكن أحد يكتبه أصلاً**.

---

## ٥. الحساب الخاص — كان يحمي نصف الأشياء

راجعتُ كل سياسات SELECT، فتبيّن أن تشخيصي السابق كان ناقصاً:
الستوري **كانت** محميّة. الثغرة الحقيقية:

```
posts     can_view ✅        profiles  ✗  السيرة والكلية والروابط مكشوفة
comments  can_view ✅        follows   ✗  أي أحد يرى من تتابع
stories   can_view ✅
```

`db/07_privacy_sm.sql`:

- **`profiles_public`** — عرض يُخفي الأعمدة الخاصة لا الصف كله،
  فالاسم والصورة يبقيان ظاهرين ليجدك الناس ويطلبوا المتابعة.
  (`security_invoker = true`، وإلا كان العرض ثقباً يتجاوز RLS.)
- **`follows_read`** — رسمك الاجتماعي لم يعد للجميع
- **`can_message()`** — يقرّر ظهور زر المراسلة
- **`messageable()`** — قائمة «رسالة جديدة»، مرتّبة: من تكلّمه فعلاً،
  ثم المتبادلون، ثم الباقي
- **`chat_folders`** — جدول للمجلّدات

---

## ٦. «Nouveau message» لا يُظهر شيئاً

سببان معاً:

```js
const refresh = debounce(…, 200);   // مؤجَّلة
refresh();                          // تُجدول، لا تُنفَّذ
const people = await api.contacts().catch(() => []);   // يبتلع الخطأ
```

الآن: **تحميل فوري** · بحث مؤجَّل فقط · **الخطأ الحقيقي معروض** مع زر
إعادة · وكل شخص يحمل وسماً (`Vous vous suivez` · `Compte privé`).

---

## ٧. المجلّدات — من ملفك الأصلي

وجدتها في ملفك:

```js
['all','pinned','unread','study','muted']   + archived
```

نقلتُ المجموعة نفسها: `Tous · Non lus · Épinglés · Études · Muets · Archivés`
بزرّ يمين على المحادثة لتصنيفها، وعدّاد على كل مجلّد، والمثبّتة أولاً.

**فرق واحد:** أصلك خزّنها في `localStorage` فكانت تضيع مع مسح المتصفح
وتختلف بين هاتفك وحاسوبك. الآن في جدول `chat_folders`.

---

## ٨. الرسائل — أسمّيها باسمها

أصلك استخدم **Supabase Realtime** (`postgres_changes`، السطر 5558).
**Neon Data API بلا WebSocket.** فهذا أفضل تقريب، لا «فوري»:

```
إرسالك          → الفقاعة فوراً، ثم تُثبَّت بصف القاعدة
محادثة مفتوحة   → 1.5s، وتسأل فقط عن الأحدث من آخر رسالة عندك
                  (listNewMessages — الجواب عادةً مصفوفة فارغة)
النافذة مخفيّة  → 15s
تبويباتك        → BroadcastChannel، تتزامن بلا أي طلب شبكة
```

وأضفتُ `teardownMessages()`: مغادرة الشاشة تُحرّر المؤقّت.
**هذا لم يكن تحسيناً للاختبار — كان تسريباً حقيقياً** يستنزف البطارية
وحصّة Neon في أي تبويب مفتوح.

---

## ٩. حفظ البروفايل «لا يحفظ»

```js
const [updated] = await db.update('profiles', row, …);
// PostgREST يردّ 200 ومصفوفة فارغة حين تُصفّي RLS الصف
// كان يبدو نجاحاً، ثم يختفي التعديل عند التحديث
```

الآن يرمي خطأً صريحاً: *«Modification refusée par la base de données»*
بدل الصمت.

---

## ١٠. أخطاء أمسكتها الاختبارات أثناء العمل

بصراحة — هذه أخطاء أدخلتُها أنا في هذه الجلسة:

| الخطأ | كيف ظهر |
|---|---|
| `let pollTimer` معلَنة مرتين | `node --check` لا يرى تصادم نطاق الوحدة → أضفتُ `tests/parse.test.mjs` |
| `profileApi.canMessage` والدالة في `messagesApi` | كان سيرمي عند فتح أي بروفايل |
| المُثبِّت يخزّن الرسائل بمفتاح الطرف الآخر | **لهذا لم يُكتشف أي خطأ ثنائي الاتجاه من قبل** — أصبح جدولاً مسطّحاً كالحقيقي |

---

## الاختبارات

```
app 47 · auth-ui 54 · campus 67 · core 57 · db-auth 39
dm 39 · feed 45 · game 65 · hub-profile 55 · layers 22
leaderboard 26 · parse 26
──────────────────────────────────────────────
542/542
```

**جديدة:**

- **`dm.test.mjs`** — محادثة من **الطرفين** على مخزن واحد: إرسال ·
  استقبال · صورة · تفاعل من الاثنين · تعديل · حذف · قراءة · مجلّدات ·
  الجلب التزايدي. *هذا ما كان غائباً حين مرّ 454 اختباراً والرسائل مكسورة.*
- **`layers.test.mjs`** — ترتيب الطبقات رقمياً
- **`parse.test.mjs`** — 26 وحدة تُحمَّل فعلاً

---

## شغّل هذا في Neon

```
db/07_privacy_sm.sql
```
ثم Data API → **Refresh schema cache**.

(إن لم تشغّل `05` و`06` بعد، فبالترتيب: 05 → 06 → 07.)

---

## ما زلتُ لم أفعله

**لم أفتح متصفحاً حقيقياً ولا Neon الحيّة.** أضفتُ فحص رسم يؤكّد أن
`src` ليس فارغاً، وهو ما كان سيمسك عطل `safeUrl` من اليوم الأول —
لكنه يبقى jsdom.

**الاختبار الحاسم عندك:**

```
غيّر صورتك → حدّث الصفحة → هل بقيت؟
أرسل صورة في محادثة → افتحها من حساب آخر → هل تظهر؟
افتح ستوري → اضغط حذف → هل ترى التأكيد؟
```

إن فشل أيٌّ منها، أرسل لي خطأ الـconsole ولن أدّعي أنه يعمل.
