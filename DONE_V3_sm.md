# نُفِّذت خطة V3

**613/613 اختبار** · **8 ملفات SQL تُحلَّل** · **29 وحدة تُحمَّل**

---

## ١. المحادثة تنزل خارج الشاشة ← أُصلحت

سلسلة الارتفاع كانت مكسورة عند `.view-inner` — لا ارتفاع إطلاقاً،
فـ`height:100%` تُحلّ مقابل `auto` وتنهار.

```css
/* الآن */
.view.full          { overflow: hidden; display: flex }
.view.full .view-inner { height: 100%; min-height: 0; padding-bottom: 0 }
.dm                 { height: calc(100dvh - var(--topbar-h)) }   /* صريح، كأصلك */
.thread             { min-height: 0; overflow: hidden }
```

استخدمتُ `dvh` لا `vh`: شريط عنوان المتصفح على الهاتف يقتطع من `vh`
فيختفي مربّع الكتابة تحته.

**والسلوك الذي طلبته:**

| الحالة | ما يحدث |
|---|---|
| فتح محادثة | **أحدث رسالة فوراً، بلا حركة** — لا أحد يريد مشاهدة تمرير عبر ٢٠٠ رسالة |
| رسالة تصل وأنت في الأسفل | تتبعها بنعومة |
| رسالة تصل وأنت تقرأ الأعلى | **لا تُزحزَح**، ويظهر زر بعدّاد «↓ ٣» |
| ترسل أنت | دائماً إلى الأسفل |

وأضفتُ ما كان ينقص: `scrollToBottom()` تُعاد بعد التخطيط **وبعد تحميل
كل صورة** — الصورة تغيّر الارتفاع عند فك ترميزها، فقفزة واحدة تقع قصيرة.

---

## ٢. زر Enregistrer ← سببان، كلاهما مُصلَح

**الأول، في القاعدة:** `06_game_sm.sql` دهس السياسة الصحيحة بـ
`role = 'student'`، فأي حساب `admin` — وحسابك منها — لا يستطيع الحفظ.
`08_fixes_sm.sql` يعيد الشرط السليم.

**الثاني، الرسالة نفسها:** كانت `'rien n'a été modifié'` رسالة احتياطية
تُعرض لأي خطأ. الآن تنقل سبب Postgres الحقيقي:

```js
: err?.status === 401 ? 'Session expirée — reconnectez-vous.'
: err?.status === 403 ? raw                    // سبب RLS الفعلي
: raw                 ? `Échec : ${raw}`
```

---

## ٣. التحدّيات لا تُسجَّل ← أخطر ما وجدت

```sql
ALTER TABLE quests FORCE ROW LEVEL SECURITY;   -- كتبتُها لمنع الغش
-- والسياسات الموجودة: SELECT فقط
-- فكل INSERT من track_quest() مرفوض صامتاً
```

`FORCE` يطبّق RLS **حتى على مالك الجدول** — وهو من تعمل باسمه دوال
`SECURITY DEFINER`. منعتُ اللعبة من العمل وأنا أحميها.

**الإصلاح** ليس حذف `FORCE` — إنه يعمل عملاً حقيقياً — بل إضافة
سياسات كتابة مقيّدة بـ`auth.user_id()`. والسجلّ يبقى **للإضافة فقط**:
لا سياسة UPDATE ولا DELETE على `xp_events`، فلا يمكن إعادة كتابة
التاريخ لتضخيم رصيد.

**وثانياً:** `wireGameEvents()` كانت تُستدعى داخل `route('hub')` فقط،
فإعجابك في الفيد يبثّ الحدث ولا أحد يستمع. الآن تُربط **عند الإقلاع**.

---

## ٤. الرسائل تحت الشريط · الطيّ

`.dm-list` كانت بـ`z-index:51` والشريط `100`. وأعدتُ الطيّ إلى منطق
ملفك: **حالة تملكها أنت** (`prefs.railFolded`) ويتذكّرها التطبيق،
والمرور يكشف التسميات فقط دون تغيير الحالة. مِن قبل كان يطوي ويفتح
حسب المسار بلا أي تحكّم — لهذا شعرتَ أنه يقاومك.

---

## ٥. اللغات — ثلاث، والإنجليزية رسمية

```
public/js/core/i18n_sm.js   ← 230 مفتاحاً × ٣ لغات، تطابق تام
```

المبدّل **أعلى اليمين** كما طلبت، يعرض `EN / FR / ع`.

**العربية مكتوبة لا مترجمة آلياً.** ومعها تفاصيل RTL الحقيقية:

```css
[dir="rtl"] .t-mono, .conv-time, .bubble-meta { direction: ltr }
```

بدونها يظهر `CS-042` كـ`042-CS` والساعة `14:30` مقلوبة.

**واكتشفتُ خطأً أثناء الفحص النهائي:** `store_sm.js` كان لا يزال
افتراضه `'fr'`، فيناقض «الإنجليزية رسمية». أُصلح.

---

## ٦. الإشعارات — مع زر التجربة

**متى يُطلب الإذن:** بعد ١٢ ثانية من الفتح، ببطاقة داخل التطبيق تشرح
أولاً. طلب `requestPermission()` أثناء التحميل هو **الأكثر رفضاً في
الويب** — الناس يضغطون Block انعكاسياً، والحظر لا يُلغى إلا من
إعدادات المتصفح، وهو ما لا يفعله أحد.

**زر التجربة** في الإعدادات، ويقول الحقيقة:

| الحالة | ما يقوله |
|---|---|
| مسموح | يُرسل إشعاراً فعلياً + «أُرسل الإشعار» |
| لم يُسأل بعد | يطلب الإذن ثم يُرسل |
| محظور | «محظورة من المتصفح — اضغط القفل في شريط العنوان» |
| غير مدعوم | يقولها بوضوح |

**ما هذا ليس:** ليس Web Push. لا مفاتيح VAPID ولا خادم. الإشعارات
تُطلق والصفحة مفتوحة — وهو ما يدعمه Neon فعلاً اليوم. تسميته «push»
كذبة؛ الـpush الحقيقي يحتاج خدمة دفع وworker يستقبل والتبويب مغلق.

---

## ٧. ضمانة الاسم — ١٥ يوماً

في القاعدة لا في المتصفح:

```sql
name_changed_at · name_change_count
track_name_change()   ← trigger، والنافذة تُصفَّر نفسها
name_change_status()  ← تُرجع كم يوماً بقي بالضبط
```

التغيير الثاني خلال النافذة يعرض تحذيراً **بعدد الأيام الحقيقي**،
ثم يسمح. اخترتُ التحذير على المنع: المنع يعاقب من يصحّح خطأً مطبعياً،
ويُلتف عليه بسهولة.

---

## أخطاء أمسكها الفحص النهائي

بصراحة، ثلاثة أدخلتُها في هذه الجلسة:

| الخطأ | كيف انكشف |
|---|---|
| `renderNav?.()` — دالة غير موجودة | فحص بنية `shell_sm.js` |
| `store_sm` افتراضه `'fr'` | فحص التكامل الحيّ |
| اختباران يطابقان **تعليقاتي** لا الكود | فشلا، فجرّدتُ التعليقات قبل المطابقة |

الثالث يستحق وقفة: كنتُ أختبر نصاً داخل `/* */` يشرح الخطأ القديم،
فيمرّ الاختبار على شرح المشكلة لا على حلّها.

---

## شغّل في Neon

```
db/08_fixes_sm.sql
```
ثم Data API → **Refresh schema cache**.

(إن لم تُشغّل السابقة: `05 → 06 → 07 → 08`.)

**بدون `08` التحدّيات لن تُسجَّل وحفظ البروفايل سيفشل** — الإصلاحان
في القاعدة لا في المتصفح.

---

## ما زلتُ لم أفعله

**لم أفتح متصفحاً حقيقياً.** jsdom لا يحسب التخطيط، فاختبار سلسلة
الارتفاع يقرأ تصريحات CSS لا البكسل الفعلي.

**اختبارك الحاسم:**

```
افتح محادثة طويلة  → هل ترى آخر رسالة ومربّع الكتابة معاً؟
اكتب ٢٠ رسالة      → هل يبقى مربّع الكتابة مكانه؟
غيّر اسمك مرتين     → هل يظهر تحذير ١٥ يوماً؟
أكمل تحدّياً        → SELECT * FROM quests WHERE user_id = '…';
اضغط زر التجربة    → هل يظهر إشعار المتصفح؟
```

إن فشل أيٌّ منها أرسل لي خطأ الـconsole، ولن أدّعي أنه يعمل.
