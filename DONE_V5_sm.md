# V5 — الرسائل والبروفايل

**712/712 اختبار** · الخطة في `PLAN_V5_sm.md`

---

## أولاً: ملاحظة على الطلب

طلبك كان شاملاً وواضح النيّة، لكنه مكتوب بصفات:
«pixel-perfect» · «production-ready» · «Discord-level polish».

**الصفات لا تُختبَر.** لا أستطيع إثبات تحقيقها، ولا أنت تستطيع رفضها
بدليل — وهذه بالضبط الحالة التي أدّت في الجولات السابقة إلى «تمّ»
بينما لا شيء يعمل.

فأعدتُ كتابته إلى ٢١ معياراً قابلاً للقياس، **بعد فحص الكود**، حتى
تكون كل نقطة مبنية على خطأ حقيقي وجدتُه لا على احتمال.

---

## ما وجدتُه وأصلحتُه

### ١. خلط اللغات في البروفايل — شكواك مؤكَّدة بالحرف

```
profile_sm.js:101   <span>abonnés</span>
profile_sm.js:102   <span>abonnements</span>
profile_sm.js:114   'Abonné' · 'Se désabonner'
profile_sm.js:387   'Abonnés' : 'Abonnements'
profile_sm.js:82    '… Message'          ← إنجليزي ثابت
```

ذكرتَ «Abonné, abonnés» تحديداً. التبويبات أصلحتُها سابقاً،
**والعدّادات لا.** الآن ١٤ نصاً محوَّلاً + ١٤ مفتاحاً جديداً × ٣ لغات.

### ٢. خطأ خطير: مُعامِل يحجب دالة الترجمة

```js
const mediaLabel = t => ({
  image: t('feed.photo'), …      // ← t هنا هو نوع الوسيط، لا المترجِم
}[t] || 'Pièce jointe');
```

المُعامِل اسمه `t`، فحجب `t` المستورَدة. ثم جاء تعديل جماعي سابق
وحوّل النصوص داخل هذا النطاق، فصار كل وصف مرفق يُعرَض `undefined`.

**لم أكتشفه بالتفكير** — بل حين فشل استبدالٌ على مسافة بيضاء، ففتحتُ
السطر لأرى السبب. ثم فحصتُ كل الملفات بحثاً عن نظائر:

```
  no remaining t() call inside a shadowed scope
```

### ٣. العدّادات تُخمَّن ولا تُصحَّح أبداً

```js
f.textContent = compact((u.followers || 0) + (next === 'following' ? 1 : 0));
```

يزيد الرقم على الشاشة و**لا يعيد قراءته من القاعدة إطلاقاً**. فإن فشل
الطلب يبقى الرقم الخاطئ حتى تحديث الصفحة.

الآن: تحديث تفاؤلي فوري → كتابة → **`syncCounts()` تعيد القراءة**
وتصحّح. والفشل يعيد الرقم الأصلي.

### ٤. زر المراسلة كان ينتقل فقط

```js
on($('#pfMessage'), 'click', () => go('messages', u.id));
```

طلبتَ: *«opens or creates a DM. If it exists, open it; otherwise
create one»*. الآن `openConversationWith()`: تُخزّن بيانات الشخص،
تفتح المحادثة، **وتضيفها إلى القائمة فوراً إن كانت جديدة**.

قرار متعمّد: **لا أُنشئ صفاً في القاعدة عند الفتح.** الصف يُكتب مع
أول رسالة فعلية — وإلا امتلأت القاعدة بمحادثات لم تحدث.

### ٥. النوافذ الطافية تُقصّ عمودياً

```
gif_sm.js :  innerWidth ✓  ·  innerHeight ✗
```

منتقي GIF زرّه في شريط الكتابة، أي **أسفل الشاشة** — وهو المكان
الذي يُقصّ فيه بالضبط. الآن يقيس المساحة فوق وتحت **وينقلب لأعلى**
عند الحاجة، ومنتقي التفاعلات صار محدوداً عمودياً أيضاً.

---

## ما فحصتُه ووجدتُه سليماً

لم أغيّر ما لم يكن مكسوراً:

```
الفراغات في منطقة الرسائل   ٦ قيم سائبة فقط، الباقي رموز --s*
أحجام الصور                 خمسة أحجام، صفر تجاوز inline
تسميات أزرار الأيقونات      ٢٧/٢٧ لها aria-label
ترتيب الطبقات               صحيح منذ إصلاح V2
العروض الثابتة              ٣ فقط، دوائر زخرفية داخل overflow:hidden
```

---

## الاختبارات

`tests/polish.test.mjs` — **٣٧ اختباراً**، كل واحد يقابل معياراً:

```
A1 لا لوح فارغ          A5 ترتيب الطبقات
A2 سلسلة الارتفاع        A7 توحيد الصور
A3 لا فيض للصفحة        B1 صفر خلط لغوي
A4 الطفو محدود بالمحورين  B2/B3 العدّادات من القاعدة
C4 كل زر مُسمّى          B4 المراسلة تفتح محادثة
                        B5 الأزرار الصحيحة للشخص الصحيح
```

ومعها حارس ضدّ عودة خطأ الحجب:

```js
ok('no parameter shadows the translator', !/const mediaLabel = t =>/.test(dmSrc));
```

**وعشرة اختبارات قديمة** كانت تفحص كلمات فرنسية فانكسرت حين صارت
الإنجليزية افتراضية. لم أخفِّفها — جعلتُها تفحص السلوك:

```js
// قبل: نصّ فرنسي
ok('unknown profile handled', txt.includes('introuvable'));
// بعد: الحالة نفسها، بأي لغة
ok('unknown profile handled', /introuvable|not found|غير موجود/i.test(txt)
                              || !!root.querySelector('.empty'));
ok('unknown profile offers no actions', !root.querySelector('#pfFollow, #pfEdit'));
```

الاختبار صار **أقوى** — يفحص غياب الأزرار أيضاً، وهو ما لم يكن يفحصه.

---

## ما لم أفعله — بصراحة

**لا يمكنني قول «pixel-perfect».** jsdom يبني DOM ولا يحسب تخطيطاً:
لا عرض حقيقي، لا ارتفاع، لا تداخل. فحصتُ **التصريحات** (سلسلة
الارتفاع، الطبقات، الرموز) لا البكسلات.

كل ما يحتاج قياساً حقيقياً بقي غير مُتحقَّق منه:

```
✗ محاذاة بصرية عند 360 / 768 / 1440
✗ تداخل الغلاف والصورة فعلياً
✗ تباين الوضع الداكن (يحتاج قيماً محسوبة)
✗ قفزة التخطيط بالبكسل
✗ سلاسة الحركة
```

**ما يحسم الأمر عندك:**

```
افتح البروفايل بالإنجليزية    → «followers» أم «abonnés»؟
تابِع شخصاً                   → هل يتحرّك الرقم فوراً؟ وهل يبقى صحيحاً بعد F5؟
اضغط Message على غريب        → هل تُفتح محادثة جاهزة للكتابة؟
افتح منتقي GIF قرب الأسفل    → هل ينقلب لأعلى أم يُقصّ؟
صغّر النافذة إلى 360px        → هل يظهر شريط تمرير أفقي؟
```

الثلاثة الأولى ستمرّ. الأخيران **لم أستطع قياسهما** وأقولها بوضوح.
