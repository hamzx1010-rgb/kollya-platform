# الأربعة المطلوبة

**759/759 اختبار** · تحقّق حيّ ١٩/١٩

---

## ١. الطيّ — حُذف بالكامل

طلبتَ حذفه بدل إصلاحه، وهذا هو القرار الصحيح. السبب أنه لم ينجح
أبداً: **ثلاث آليات تقرّر عرضاً واحداً**، وتتعارض أثناء الانتقال —

```
قاعدة حسب المسار  ·  مؤقّت طيّ تلقائي ٢٫٢s  ·  كشف بالمرور
```

فالشريط في منتصف التوسّع بينما العناصر ما زالت مركزية، وتبدو
الأيقونات مسحوقة. حاولتُ التوفيق بينها مرتين وفشلت مرتين.

**ما حُذف:**

```
JS      syncRail المعقّدة · toggleRail · syncFoldButton · wireRailPeek
        · AUTO_FOLD_MS · FOCUS_ROUTES · prefs.railFolded
HTML    زر #btnFold · زر #btnDmFold
CSS     ~٦٧٠٠ حرف — كل قواعد data-rail="collapsed" و data-peek و dm-folded
```

الشريط الآن **مفتوح دائماً**، على كل مسار، بلا استثناء.

## ٢. المنشورات أوسع

```
--feed-w:  620px → 720px      (و 680 → 800 على الشاشات الكبيرة)
```

٦٢٠px كانت مضبوطة لعمود ضيّق. ومع بقاء الشريط مفتوحاً هناك متّسع،
والصور في عمود ٦٢٠px كانت تبدو مصغّرات.

## ٣. صندوق المحادثة — وجدتُ السبب بالضبط

```css
.conv { display: flex; padding: …; }   /* لا width */
```

و`.conv` عنصر **`<button>`** — والأزرار عرضها `auto` افتراضياً،
فالصف **ينكمش حول نصّه**. لذلك الظل عند النقر يغطّي نصف الصندوق فقط،
ويبقى الشريط الفارغ الغريب الذي رأيتَه على اليمين.

وثلاث مشاكل أخرى ورثها من كونه زراً:

```css
width: 100%;              /* يملأ صندوقه */
box-sizing: border-box;   /* الحشو داخل العرض لا فوقه */
text-align: start;        /* لا توسيط الأزرار */
font: inherit;            /* خط التطبيق لا خط المتصفح */
```

**أبقيتُه `<button>`** — لأنه يعمل بلوحة المفاتيح ويُعلَن لقارئ الشاشة،
وتحويله إلى `<div>` كان سيخسر ذلك.

## ٤. عدّاد التحدّيات — «شيء في الطريق»

```css
.quest { align-items: center }   /* الأيقونة ٣٤px تحدّد ارتفاع الصف */
.bar   { height: 4px; margin-top: 5px }
```

التوسيط العمودي يضغط الشريط بين النص وحافة الصف، فيبدو كأن شيئاً
يعترضه. الآن:

```css
.quest      { align-items: flex-start }   /* الأيقونة لا تسحق الشريط */
.quest .bar { height: 6px; margin-top: var(--s2) }
.bar-fill   { background: var(--grad) }
```

وأزلتُ الأنماط السطرية التي كانت تصارع الملف، **وأضفتُ**:

```html
role="progressbar" aria-valuenow="2" aria-valuemax="3"
```

فقارئ الشاشة يقول «٢ من ٣» بدل تجاهل الشريط تماماً.

---

## التحقّق

```
١. الطيّ محذوف          ٦/٦ ✓   (بما فيها: يبقى مفتوحاً بعد التنقّل)
٢. المنشورات أوسع       ١/١ ✓
٣. صف المحادثة          ٥/٥ ✓   (بما فيها: ما زال زراً حقيقياً)
٤. عدّاد التحدّيات       ٧/٧ ✓
```

وأضفتُ **١٢ حارساً دائماً** في `quality.test.mjs`، فلا يعود الطيّ
تسلّلاً ولا ينكمش صف المحادثة ثانيةً.

**وخمسة اختبارات قديمة** كانت تؤكّد أن الشريط **ينطوي** — قلبتُها
لتؤكّد أنه **يبقى مفتوحاً**، وهو السلوك المطلوب الآن.

---

## ما لم أتحقّق منه

**لم أفتح متصفحاً.** jsdom لا يحسب تخطيطاً، فقِستُ التصريحات
(`width:100%` موجود، الارتفاع ٦px، `role` صحيح) لا البكسل المرسوم.

**جرّب:**

```
تنقّل بين الصفحات   → هل يبقى الشريط مفتوحاً دائماً؟
مرّر على محادثة     → هل يغطّي الظل الصندوق كاملاً؟
افتح الـHub         → هل الشريط واضح تحت النص؟
انظر إلى منشور بصورة → هل صار أوسع؟
```
