# V7 — المشاكل الخمس

**837/837 اختبار** · الخطة في `PLAN_V7_sm.md`

---

## ١ · صفحة الرسائل لا تظهر حتى تضغط الفراغ

**وجدتُ السبب، وهو خطأ صياغة CSS من صنعي:**

```css
.dm,                    ← السطر 2370
                        ← محدِّد معلّق بلا كتلة
@media (min-width: 1400px) {
```

فاصلة تنتظر محدِّداً ثانياً فيليها `@media`. CSS يعتبر هذا خطأ صياغة،
**فيتجاهل قاعدة `.dm` ويبتلع الـ`@media` معها**. تفقد اللوحة ارتفاعها
الصريح ولا تظهر حتى يُجبَر المتصفح على إعادة الحساب — وهو ما يحدث حين
تضغط في الفراغ.

سببه سكربت حذف الطيّ: أزال شطراً من قائمة محدِّدات وترك الفاصلة.

**والأهم:** أضفتُ `tests/css.test.mjs` — **٢٨ فحصاً بنيوياً** يوازن
الأقواس ويكشف المحدِّدات المعلّقة والقواعد الفارغة والمتغيّرات غير
المعرَّفة. الاختبارات القديمة تقرأ التصريحات بـregex، وهذا يطابق نصاً
داخل قاعدة رماها المتصفح أصلاً.

**وأمسك ثلاثة أخطاء أخرى فوراً:**

```
--s7 مستخدمة وغير معرَّفة   → حشو ينهار إلى صفر بصمت
@media فارغتان              → بقايا حذف الطيّ
محدِّد معلّق ثانٍ            → أنشأتُه أثناء إصلاح المسجّل الصوتي
```

الأخير يستحق الوقفة: **الاختبار أمسك خطأً كنتُ أرتكبه في اللحظة نفسها.**

---

## ٢ · المقاسات تصغر عند تقسيم الشاشة

السبب: عرض ثابت + عشر نقاط توقّف. الصفحة تبقى بحجمها حتى تعبر عتبة ثم
**تقفز** — وبين العتبتين تبدو مصغّرة.

```css
--feed-w: 720px  →  clamp(300px, 88vw, 780px)
--fs-2xl: 26px   →  clamp(21px, 2vw + 14px, 26px)
```

**والأهم: container queries.**

```css
.dm { container-type: inline-size; container-name: dm; }
@container dm (max-width: 720px) { … }
```

الفرق جوهري: `@media` تقيس **النافذة**، و`@container` تقيس **اللوحة
نفسها**. في نافذة مقسومة قد تكون النافذة ضيّقة واللوحة واسعة أو العكس —
فالسؤال الذي تطرحه media query خاطئ من الأساس. هذا بالضبط ما يكسر عندك.

**نصّ الجسم يبقى ثابتاً عمداً** — الخط السائل يضرّ القراءة ويخالف
معايير WCAG لتكبير النص.

---

## ٣ · الرسائل كإنستغرام

اختبرتُ التوجيه أولاً فوجدتُه سليماً — المشكلة كانت CSS المكسورة.
لكن أضفتُ السلوك الذي طلبته:

```
ضغط «Messages» ولوحة واسعة  →  القائمة + أحدث محادثة جنبها
ضغط «Messages» ولوحة ضيّقة  →  القائمة وحدها، كإنستغرام
ضغط على محادثة              →  تفتح على أحدث رسالة
```

**وخطأ حقيقي كشفه الاختبار:**

```js
($('#dm')?.clientWidth || innerWidth) > 720
                       ↑ clientWidth = 0 قبل أول تخطيط
                         فيسقط على عرض النافذة ويأخذ المسار الواسع
```

على هاتف، لوحة غير مقيسة كانت تُعامَل كواسعة. الآن `?? 0`: **اللوحة
غير المقيسة تُعتبر ضيّقة** — عرض القائمة آمن دائماً، وفتح محادثة فوقها
ليس كذلك.

---

## ٤ · GIF والتسجيل يفتحان للأسفل

```js
// قبل: ينقلب لأعلى فقط إن ضاقت المساحة تحت
const openUp = spaceBelow < r.height + pad && spaceAbove > spaceBelow;

// بعد: لأعلى افتراضياً — الزر في شريط الكتابة أسفل الشاشة
const openUp = spaceAbove >= fits(r.height) || spaceAbove > spaceBelow;
```

**والتسجيل الصوتي كان يستبدل شريط الكتابة:**

```css
#composerWrap.recording .composer { display: none }   ← يختفي ما كتبتَه
```

الآن يطفو **فوقه**، والشريط يبهت ويبقى — فمسودّتك تنجو.

---

## ٥ · بطاقة الإنجاز

كان toast فرنسياً ثابتاً نجا من كل مسح الترجمة لأنه داخل قالب نصّي.

الآن بطاقة حقيقية: **علامة صحّ ترتسم بنفسها** · الاسم مشطوباً ·
XP يعدّ تصاعدياً · شريط اليوم يتقدّم ٢/٣ → ٣/٣ · وطابور فلا تتراكم
بطاقتان.

وجواباً على سؤالك «كيف يقرأ الموقع المهمّة المكتملة؟» — **لا يستطلع**.
`game_sm` يبثّ الحدث حين تؤكّد Postgres الكتابة، فالبطاقة تظهر للتقدّم
المحفوظ فعلاً لا للمتفائل.

ومع `prefers-reduced-motion` تظهر بلا حركة — تقول ما حدث دون أن تتحرّك.

---

## ما لم أتحقّق منه

**لم أفتح متصفحاً.** jsdom لا يحسب تخطيطاً، فلم أقِس:

```
✗ هل تبدو النافذة المقسومة صحيحة بالبكسل
✗ هل ينقلب منتقي GIF فعلاً لأعلى على شاشتك
✗ هل حركة علامة الصحّ سلسة
```

**جرّب:**
```
قسّم النافذة للنصف   → هل تسيل الصفحة أم تقفز؟
اضغط Messages        → هل تظهر فوراً بلا ضغط في الفراغ؟
افتح منتقي GIF        → هل يفتح لأعلى؟
سجّل صوتاً            → هل يبقى ما كتبتَه؟
أكمل تحدّياً          → هل ترى البطاقة والصحّ يرتسم؟
```
