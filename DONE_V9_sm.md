# V9 — الصفحات الناقصة في الهاتف

---

## 1. تصحيح: كنت مخطئاً مرتين

**المرة الأولى** (في ملخّصي السابق): قلت إن Hub مخفي على الهاتف بسبب «حدّ
5 تبويبات». قِست فوجدته ظاهراً بعرض 64px، فقلت لك إنني كنت مخطئاً.

**المرة الثانية**: كنت مخطئاً في التصحيح. القاعدة
`.nav-item[data-nav="hub"] { display: none }` **كانت موجودة فعلاً** — لكن
ملف `layout_sm.css` الذي قِسته كان قد رجع إلى نسخة أقدم قبل أن أبدأ هذه
الجلسة (تفصيل ذلك في القسم 5). كلامي الأول كان صحيحاً.

آسف على التخبّط. الفحص النهائي مبني على الملفات المدموجة الصحيحة.

---

## 2. المشكلة الحقيقية

أربع صفحات **مبنية بالكامل ومربوطة بالراوتر** لكن لا مدخل لها إطلاقاً من
الهاتف:

| الصفحة | الحالة | الكود |
|---|---|---|
| **events** | `display:none` — لا رابط ولا اختصار | كامل: `eventCard` · `attend()` · XP |
| **qa** | `display:none` | كامل: `openAsk` · `openQuestion` |
| channels | `display:none` | كامل |
| saved | `display:none` | كامل |

السبب سطر واحد في `layout_sm.css`:
```css
.nav-item:not(.primary) { display: none; }
```
الصفحات موجودة وتعمل — المفقود **الطريق إليها فقط**.

---

## 3. الحل المنفَّذ

### أ) Hub نزل إلى الصفحة الرئيسية، ومكانه صار «الحرم»

الشريط السفلي بقي **6 تبويبات** (55px لكل واحد على 360px — فوق حدّ اللمس
48dp؛ سبعة تعني 47px وهو تحت الحد):

```
الرئيسية · استكشاف · الرسائل · الإشعارات · الحرم · حسابي
```

«الحرم» يفتح شريط تبويبات يضم الصفحات الخمس:
```
استكشاف │ الفعاليات │ الأسئلة │ القنوات │ المحفوظات
```
شريط أفقي قابل للتمرير — لأن خمسة تبويبات بأيقونات بـ `flex:1` تعطي 46px
لكل واحد والنصوص تنقطع في منتصف الكلمة. قِست: لا نصّ مقطوع الآن.

### ب) بطاقة التقدّم المضغوطة (اخترت «سطر واحد»)

سطر واحد أعلى قائمة المنشورات:
```
Lv 3  ▓▓▓▓▓▓░░░░  175/214 XP   🔥4   🏆1/3   ›
```
- يقرأ `stats()` — **نفس مصدر Hub**، فلا يمكن أن يختلفا
- يتحدّث فوراً مع `game:xp` / `game:quests` / `game:streak`
- الضغط ينقل إلى `#/hub` الكامل
- على شاشة 360px يختفي جزء `175/214 XP` (الشريط يقول نفس الشيء)
- `role="progressbar"` مع `aria-valuemax` لقارئ الشاشة

على الحاسوب: Hub يبقى في الشريط الجانبي، والصفحات الأربع تبقى صفوفاً
مستقلة، ولا شيء مكرَّر.

---

## 4. ثلاثة أخطاء وجدتها في الصورة ولم تطلبها

فتحت `shots/N5` ونظرت إليها، فظهر في صفحة الفعاليات العربية:

1. **`à venir` و `vos inscriptions`** — فرنسي داخل صفحة عربية. كانت
   مكتوبة نصاً صريحاً في `campus_sm.js`. صارت `قادمة` و `تشارك فيها`.
2. **`أنشئفعالية`** بلا مسافة — السبب `.hero-cta-txt br { display: none }`
   يحذف فاصل السطر **دون أن يترك مسافة**. صار `br::after { content: " " }`.
3. **شارة `Campus`** غير مترجمة ← `الحرم`.

وأثناء الإصلاح اكتشفت أن `events.upcoming` و `qa.eyebrow` و `qa.unanswered`
**كانت معرَّفة مرتين** في كل لغة — والمفتاح المكرّر يدهس الأول بصمت.
حذفت التكرار واستعملت المفاتيح الأصلية.

---

## 5. حادثة فقدان بيانات — يجب أن تعرفها

في بداية هذه الجلسة اكتشفت أن **كل عمل V8 (الإشعارات 24/7) كان قد رجع**:
- `native_sm.js` و `gestures_sm.js` **محذوفان**
- كل تعديلات V8 على 8 ملفات JS/CSS مفقودة
- الـ 16 ملف جافا مفقودة
- `background.test.mjs` محذوف، و`persist.test.mjs` رجع لقراءة `/tmp`

`apk/Koliya-1.2.apk` كان النسخة الوحيدة الباقية. استخرجت منه كل شيء
ودمجته يدوياً مع عمل V9 (ملف بملف — `feed_sm.js` و `i18n_sm.js` و
`layout_sm.css` عدّلتها الجلستان معاً فاحتاجت دمجاً لا استبدالاً).

**النتيجة:** V8 و V9 موجودان معاً الآن ومُختبَران.

**ما زال ناقصاً:** الـ 16 ملف جافا المصدرية. الكود **مترجَم داخل
`Koliya-1.2.apk` ويعمل** (تحقّقت: `SyncService`, `SyncWorker`,
`NeonClient`, `SyncBridge`, `AlarmScheduler` كلها في `classes.dex`)، لكن
المصدر يحتاج استخراجاً من جديد قبل أي بناء قادم. **لم أبنِ APK جديداً في
هذه الجلسة** — تغييرات V9 كلها ويب، و`Koliya-1.2.apk` الحالي سليم.

---

## 6. الأرقام

```
tests/run.sh           851/851   jsdom
tests/browser/run.sh   245/245   Chrome حقيقي
  background 47   (مستعاد)
  live       74
  nav        64   ← جديد
  persist    38   (مُصلَح: يولّد PNG بنفسه بدل /tmp)
  sound      22
tests/sql/run.sh       تخطّى — PostgreSQL أُزيل من الآلة
```

`nav.test.mjs` يختبر **إمكانية الوصول** لا الشكل: يضغط ما يراه الطالب
فعلاً ويتحقّق أين وصل. العشر صفحات كلها يجب أن تُفتح بالنقر وحده.

**لم أشغّل اختبار SQL** — الحزمة أُزيلت من الآلة وإعادة تنصيبها لا تخدم
تغييراً كله CSS/JS. لم ألمس قاعدة البيانات.

## 7. الصور
`N1-home-strip` · `N2-campus-events` · `N3-campus-qa` · `N4-home-arabic` ·
`N5-campus-arabic` · `N6-desktop` · `N7-home-dark` — نظرت إليها بنفسي.

## 8. قياسات
```
360px: 6 تبويبات × 55px  · لا فيض · لا نصّ مقطوع
412px: 6 تبويبات × 64px  · البطاقة 22→380 داخل الحدود
1280px: 9 صفوف · Hub موجود · لا تكرار
#/explore/qa → يتحوّل إلى #/qa  (رابط واحد لكل صفحة)
#/events · #/qa · #/channels · #/saved → تعمل مباشرة
```
