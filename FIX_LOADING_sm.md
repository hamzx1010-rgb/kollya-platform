# التحميل اللانهائي — مُصلَح

## ما كان يحدث

`resolveSession()` كانت تنتظر رد Neon **بلا مهلة**:

```js
const state = await initAuth();   // إن لم يردّ أبداً → دوران للأبد
```

أي شيء يمنع الرد — إنترنت بطيء، حاسوب Neon نائم، أو **نطاق غير موثوق**
— يترك الشاشة تدور بلا نهاية وبلا رسالة.

---

## ما أصلحته

**١. مهلة 8 ثوانٍ على فحص الجلسة**
شاشة دخول تظهر مبكراً أفضل من شاشة تحميل لا تنتهي — على الأقل يستطيع
الطالب التصرّف.

**٢. رسالة بعد 3 ثوانٍ** — "Connexion au serveur…" حتى يعرف أن شيئاً يحدث.

**٣. شاشة إنقاذ بعد 15 ثانية** — مع زر "Réessayer".

**٤. كشف الملفات المفقودة**
لو 404 لأي ملف JS، الشاشة تعرض **مساره بالضبط** بدل الدوران:
```
Fichier introuvable :
http://localhost:8099/js/core/auth_sm.js
```
هذا يخبرك فوراً أي ملف نسيته أو أخطأت في اسمه.

**٥. 🐛 وأصلحت بقاً أدخلته أنا**
كتبت `dispatchEvent(...)` بدل `window.dispatchEvent(...)`. الأول غير
معرّف داخل نطاق الوحدة ويرمي `ReferenceError` — **وهو بالضبط ما كان
يُوقف الإقلاع**. اكتشفه الاختبار حين حاكيت شبكة ميتة.

---

## اختبرت الحالتين

```
إقلاع عادي         :  boot → login خلال 1.5 ثانية  ✓
شبكة ميتة تماماً   :  boot يختفي، login يظهر       ✓
380/380 اختبار                                      ✓
```

---

## 🔴 لكن السبب الأرجح عندك

**النطاق غير مضاف في Neon.**

اذهب إلى **Neon Console → Auth → Trusted domains** وأضف بالضبط:

```
http://localhost:8099
```

> لاحظ: `http` لا `https`، ومع رقم المنفذ، وبلا شرطة مائلة في النهاية.
> إن كنت تستعمل منفذاً آخر (5500 مع Live Server مثلاً) أضفه هو.

بدونه يرفض Neon الطلب، وقبل الإصلاح كان التطبيق يدور بلا نهاية.
الآن سيُظهر شاشة الدخول مع تنبيه.

---

## كيف تعرف السبب الحقيقي

**افتح Console بـF12** وأعد تحميل الصفحة. سترى واحدة من هذه:

| الرسالة | المعنى | الحل |
|---|---|---|
| `404 ... /js/core/xxx_sm.js` | ملف ناقص أو اسمه خطأ | راجع المسار |
| `CORS` أو `MISSING_ORIGIN` | النطاق غير موثوق | أضفه في Neon |
| `auth timeout (8000ms)` | Neon بطيء أو محجوب | تحقّق من الاتصال |
| `Failed to fetch` | لا إنترنت | — |
| `Cannot use import statement` | فتحت بـ`file://` | استعمل `http://` |

**أرسل لي ما يظهر** وأحدّد السبب بدقة.

---

## تذكير: لا تفتح الملف مباشرة

```bash
cd koliya/public
python3 -m http.server 8099
```
ثم `http://localhost:8099`

النقر المزدوج على `index_sm.html` يعطي `file://` — ووحدات ES **لا تعمل**
هناك أبداً، مهما كان الكود صحيحاً.

---

## الملفات المحدّثة

- `public/index_sm.html` — شبكة الأمان
- `public/js/app_sm.js` — المهلة + إصلاح `dispatchEvent`

حمّلهما من جديد إن كنت قد نسختهما بالفعل.
