# اللعبة — كيف تعمل الآن

## أولاً: أين أضع الملفات؟

**لا شيء.** كل الملفات مكتوبة في مكانها الصحيح داخل مجلد `koliya/`.
لا يوجد ملف تنقله بيدك.

الجديد في هذه الجلسة:

```
public/js/core/game_sm.js      ← محرّك اللعبة
public/js/core/api_sm.js       ← الجسر إلى Neon
public/js/core/media_sm.js     ← الصور تدخل قاعدة البيانات مباشرة
public/js/core/people_sm.js    ← ذاكرة الملفات الشخصية
db/05_upgrade_sm.sql           ← وسائط + استطلاعات + كتابة
db/06_game_sm.sql              ← اللعبة داخل Postgres
tests/game.test.mjs            ← 65 اختباراً للعبة
tests/fake_api.mjs             ← بيانات الاختبار
```

**الشيء الوحيد المطلوب منك:** شغّل ملفّي SQL في Neon SQL Editor،
بهذا الترتيب:

```
1. db/05_upgrade_sm.sql
2. db/06_game_sm.sql
```

(بشرط أنك شغّلت `01_schema.sql` و `02_policies.sql` من قبل.)

---

## ثانياً: هل كان الأمر يعمل؟ لا

سألتَ سؤالاً صحيحاً. هذا ما وجدته:

```
trackQuest()  ← مُصدَّرة من hub_sm.js
              ← عدد من يستدعيها في كل التطبيق: صفر
```

الإعجاب لا يخبر التحدّيات بشيء. النشر لا يخبرها. التعليق لا يخبرها.
والأسوأ: **لا يوجد سطر واحد يُنقص الـstreak**. كان يستطيع الصعود فقط.

وكانت التحدّيات في `localStorage`، أي:
مسح المتصفح = مسح تقدّمك · هاتفك يعرض تحدّيات غير حاسوبك ·
سطر واحد في الـconsole يمنحك 10000 XP.

**كنت محقاً تماماً: مرئيات ثابتة، لا ربح ولا خسارة.**

---

## اللعبة كما طلبتها

### الحلقة

```
تُنهي التحدّيات الثلاثة  →  الـstreak يستمر  →  XP
                                                 ↓
                                    الترتيب في اللوحة
                                                 ↓
                            تظهر أولاً في «طلاب لتكتشفهم»
                                                 ↓
                                    متابعون حقيقيون
```

### الـstreak — يخسر فعلاً

| ما حدث | النتيجة |
|---|---|
| أنهيت تحدّيات اليوم بعد الأمس | **+1** |
| فاتك يوم واحد، والتجميدة متاحة | تُنفَق التجميدة، **الـstreak ينجو** |
| فاتك يوم واحد، والتجميدة مستهلكة | **صفر** |
| فاتك يومان أو أكثر | **صفر** |

**تجميدة واحدة كل شهر ميلادي، تُنفَق تلقائياً.** لا تحتاج أن تتذكّرها.
تنقذك مرة، بصمت، ثم تعود أول الشهر التالي.

رقمك القياسي (`streak_best`) يبقى حتى بعد الانهيار — تخسر السلسلة،
لا تاريخك.

الحساب يقع **عند فتح التطبيق**، فتعرف فوراً، لا بعد أسبوع.

### XP — المقياس السريع كما اخترت

```
افتتاح يوم كامل   20 XP   ← الأكبر عمداً: الحضور يغلب الكمية
يوم من السلسلة     7 XP
منشور              8 XP
إجابة على سؤال     6 XP   ← المساعدة تدفع أكثر من الكلام
إنشاء فعالية       6 XP
ستوري              4 XP
تعليق              3 XP
حضور فعالية        3 XP
إعجاب مُستلَم       1 XP   ← بحدّ أقصى 10 يومياً
```

**اليوم الكامل ≈ 35 XP → الألف ≈ شهر.**

الحد على الإعجابات مقصود: منشور واحد ينتشر يجب ألّا يتجاوز شهراً من
الالتزام. أقصى ما يجنيه الانتشار في يوم (10 XP) أقل من يوم عادي كامل (35).

**الألف تعني الآن: «هذا الشخص حضر شهراً».** وهذا ما يجعل الرقم يستحق النظر.

### المستويات — أبقيتها وأعدت معايرتها

المنحنى القديم `80 × lvl^1.35` مع XP المتضخّم كان يجعل الجميع المستوى 20
خلال أسبوعين، فلا يعني الرقم شيئاً. الآن `45 × lvl^1.42`:

- 1000 XP ← المستوى 7 تقريباً
- أسبوعان من العمل ← أقل من المستوى 12

### الجائزة — ظهور، لا متابعين وهميين

اخترت «الظهور فقط»، وهو الصواب:

- المراكز 1–3 لها **شارة** ذهبية/فضية/برونزية على البروفايل وفي الاستكشاف
- «طلاب لتكتشفهم» **مرتّبة حسب XP** — تصعد، فتُرى
- المتابعون الذين تكسبهم **أشخاص حقيقيون قرّروا متابعتك**

كان الأسهل أن أُنشئ صفوف متابعة تلقائية. كان ذلك سيجعل الرقم كذبة:
متابع لم يضغط شيئاً أبداً.

---

## لماذا لا يمكن الغش

القواعد داخل Postgres، لا داخل المتصفح:

```sql
-- لا سياسة INSERT على xp_events للطالب إطلاقاً
CREATE POLICY xp_read_own ON xp_events FOR SELECT ...  -- قراءة فقط

-- الطريق الوحيد: دالة تتحقّق من المبلغ
award_xp()  →  RAISE EXCEPTION 'invalid xp amount' إذا > 100

-- وفهرس يمنع الدفع مرتين لنفس الشيء
CREATE UNIQUE INDEX idx_xp_once ON xp_events(user_id, kind, ref_type, ref_id)

-- ومُشغِّل يرفض تعديل xp أو streak أو role باليد
trg_guard_profile_progress
```

`xp_events` سجلّ لا يُمحى: كل نقطة لها سبب مسجَّل. إذا بدا رقم خاطئاً،
`recompute_xp()` يعيد بناء المجموع من السجلّ.

---

## الاختبارات

```
tests/game.test.mjs   65/65
المجموع              454/454
```

الاختبارات تشمل **كل فرع من فروع الخسارة**:
انهيار بعد يومين · التجميدة تنقذ مرة · التجميدة لا تعمل مرتين في الشهر ·
تعود أول الشهر · الرقم القياسي ينجو · فجوة 3 أيام لا تُنقَذ.

وتشمل ما فشل سابقاً — أن الوحدات **تستدعي** `act()` فعلاً:

```js
ok('feed fires the game on post',  /act\('post'/.test(src('feed_sm.js')));
ok('the engine is wired at boot',  /wireGame\(\)/.test(app));
```

---

## ما وجدته أثناء العمل

خطآن حقيقيان كشفتهما الاختبارات، لا افتراضات:

1. **`#hubRoot` لم يوجد قط.** `refreshStats()` كانت تجلب الأرقام
   الحقيقية من Neon ثم **ترميها** لأن الحارس يفحص عنصراً غير موجود.
   الشارات لم تكن تظهر أبداً.

2. **شارة غير المقروء اختفت** من بطاقة القناة أثناء إعادة كتابتي
   لـ`campus_sm.js`. انحدار سبّبته أنا.

---

## ما لم أفعله

**لم أشغّل هذا على متصفح حقيقي ولا على Neon الحية.** الاختبارات تعمل في
jsdom. منطق الـstreak مُختبَر عبر نسخة JS أمينة من دالة SQL — والنسختان
يجب أن تتطابقا؛ إن غيّرت واحدة، غيّر الأخرى.

**الاختبار الحقيقي الذي يحسم الأمر:**

```
أنهِ التحدّيات الثلاثة  →  حدّث الصفحة  →  الـstreak ما زال هناك
                         →  افتح Neon SQL Editor:

SELECT streak, streak_best, xp, freeze_month FROM profiles
WHERE username = 'اسمك';

SELECT kind, amount, day FROM xp_events ORDER BY id DESC LIMIT 10;
```

إذا رأيت صفوفاً في `xp_events`، فاللعبة تعمل.
إذا لم ترَ، أخبرني بالخطأ من الـconsole ولن أدّعي أنها تعمل.
