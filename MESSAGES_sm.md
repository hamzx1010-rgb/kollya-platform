# صفحة الرسائل — كل ما فيها

**806/806 اختبار** · `db/09_requests_sm.sql` جديد

---

## أولاً: خطأ «you don't have permission»

**هذا خطئي، ورسالتي أنا.** يظهر حين ترفض قاعدة البيانات تحديث
البروفايل:

```js
throw new DbError('__RLS_DENIED__', 403);   // ← errorText تترجمها
```

**السبب:** `06_game_sm.sql` كتب سياسة خاطئة:

```sql
WITH CHECK (id = auth.user_id() AND role = 'student')
                                   ↑ حسابك دوره 'admin' فيُرفض
```

**الحل:** شغّل هذا في Neon SQL Editor — كتبتُه سابقاً ولم تشغّله بعد:

```
db/08_fixes_sm.sql
```

ثم Data API → **Refresh schema cache**. سيعمل الحفظ فوراً.

---

## ثانياً: طلبات الرسائل — كما في إنستغرام

كنتَ محقاً أنني لم أضفها. الآن موجودة، **والقاعدة تفرضها لا المتصفح**.

### القاعدة

```
تتابعه أو يتابعك        →  محادثة عادية، مباشرة
غريب يكتب لك           →  يذهب إلى «الطلبات»
تقبل                    →  ينتقل إلى صندوقك
تتجاهل                  →  لا يعرف أبداً، ولا يستطيع الاستمرار
```

### ثلاث رسائل كحدّ أقصى

طلب معلّق يسمح بـ**٣ رسائل فقط**. بلا هذا الحدّ يستطيع شخص غير مرغوب
ملء تبويب طلباتك بخمسين سطراً لم تطلبها. ثلاثة تكفي ليقول من هو ولماذا
يكتب.

### الرفض صامت — وهذه أهم نقطة

```sql
IF FOUND AND existing.state = 'declined' THEN
  RETURN NULL;   -- تُسقَط الرسالة، والمرسِل يراها عنده بلا أي خطأ
END IF;
```

لا إشعار، لا «تمّت القراءة»، لا شيء يكشف قرارك. **إخبارهم يُفسد الغرض**
ويدعوهم للمحاولة من زاوية أخرى، ويُحرج من لم يُقبَل بعد.

وسياسة RLS تمنع المرسِل من الاستعلام عن حالته:

```sql
USING (owner_id = auth.user_id() OR is_admin())
```

### ما يظهر في البطاقة

```
الصورة والاسم و@المعرّف · الكلية
الصلات المشتركة  ← أقوى إشارة لمعرفة من هذا
أول رسالة (٣ أسطر كحدّ)
«+٢ أخرى» إن أرسل أكثر
[قبول] [رفض] [قراءة]
```

**القراءة لا تعني القبول.** تفتح المحادثة وترى شريطاً بدل مربّع الكتابة:
الرد **هو** القبول، فيجب أن يكون قراراً صريحاً لا حادثاً.

### وقبل أن ترسل أنت

زر المراسلة على البروفايل يخبرك مسبقاً:

> «ستصل كطلب حتى يقبلوا»

معرفة ذلك بعد الصمت أسوأ من معرفته قبل الكتابة.

---

## ثالثاً: كل ميزات صفحة الرسائل

### الإرسال والاستقبال
```
نصّ · صور · فيديو · ملفات · رسائل صوتية · GIF
إرسال متفائل: الفقاعة فوراً، ثم تُثبَّت من القاعدة
فشل الإرسال يزيل الفقاعة ويخبرك — لا يدّعي النجاح
تحديث كل 1.5s بجلب تزايدي · 15s والنافذة مخفيّة
مزامنة تبويباتك عبر BroadcastChannel بلا أي طلب شبكة
```

### على كل رسالة
```
تفاعلات (٦ رموز SVG)     ردّ مع اقتباس وقفز للأصل
تعديل (سهم لأعلى)         حذف للطرفين
تحويل لشخص آخر            نسخ
علامة قراءة ✓✓            «يكتب الآن»
```

### الوسائط
```
لصق لقطة شاشة (Ctrl+V)    سحب وإفلات
محرّر صور قبل الإرسال      تكبير بالضغط
تسجيل صوتي بموجة قابلة للسحب
منتقي GIF حقيقي (١٦ صورة متحرّكة · Tenor عند وجود مفتاح)
```

### التنظيم
```
٧ مجلّدات: الكل · الطلبات · غير مقروء · مثبّت · دراسة · مكتوم · أرشيف
زرّ يمين على المحادثة لتصنيفها · محفوظة في القاعدة لا المتصفح
بحث في القائمة · بحث داخل المحادثة
تصدير المحادثة نصّاً (محلّي، لا يغادر جهازك)
```

### لوحة المعلومات
```
الوسائط المشتركة · الملفات · الروابط · الرسائل الصوتية
اسم مستعار (تراه أنت فقط) · ٥ سمات للمحادثة
كتم · إفراغ · حذف · حظر · إبلاغ
```

### الويب لا الهاتف
```
Enter يرسل · Shift+Enter سطر جديد
↑ يعدّل آخر رسالة · Esc يلغي
زرّ يمين قائمة كاملة · نقرة تُظهر أدوات الرسالة
Mic و Send زرّان منفصلان
```

---

## ما أصلحتُه أثناء البناء

**خطأ حقيقي كشفه الاختبار:** بعد الرفض، كانت المحادثة **تبقى في
الصندوق**. الفلتر كان يستبعد `pending` فقط لا `declined` — أي أن
الميزة كانت تفشل في الشيء الوحيد الموجودة لأجله. أُصلح في SQL
والواجهة والمُثبِّت معاً.

**واختبار كتبتُه خاطئاً:** افترضتُ أن قبولي لشخص يفتح لي الكتابة إليه.
الاتجاه معكوس — **قبولهم لي** هو ما يفتح القناة.

---

## الترتيب في Neon

```
05_upgrade → 06_game → 07_privacy → 08_fixes → 09_requests
```

**`08` يصلح خطأ الصلاحية عندك الآن.** و`09` يضيف الطلبات.

---

## ما لم أتحقّق منه

**لم أفتح متصفحاً**، ولم أشغّل هذا على Neon الحيّة. منطق الطلبات
مُختبَر عبر مُثبِّت **يحاكي المُشغِّل حرفياً** — فإن غيّرت أحدهما،
غيّر الآخر.

**الاختبار الحاسم:** أنشئ حسابين، اجعل أحدهما يراسل الآخر دون متابعة،
وتأكّد أن الرسالة تصل إلى «الطلبات» لا إلى الصندوق.
