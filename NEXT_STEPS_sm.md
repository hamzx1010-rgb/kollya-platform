# ✅ القيمتان مضبوطتان — لكن إعدادان ناقصان في Neon

اختبرت الروابط **فعلياً** مقابل مشروعك الحيّ. النتائج:

| الفحص | النتيجة |
|---|---|
| Data API يستجيب | ✅ |
| Data API يرفض غير المسجّلين | ✅ (`missing authentication credentials`) |
| خدمة Auth تعمل | ✅ (`/ok` → `{"ok":true}`) |
| مفاتيح JWKS متاحة | ✅ خوارزمية **EdDSA / Ed25519** |
| **تسجيل الدخول بالبريد** | ❌ **معطّل** |
| **النطاقات الموثوقة** | ⚠️ تحتاج ضبطاً |

---

## 🔴 مطلوب منك — إعدادان في لوحة Neon

### 1. فعّل تسجيل الدخول بالبريد

الخادم يردّ حرفياً:

```json
{ "code": "EMAIL_AND_PASSWORD_IS_NOT_ENABLED",
  "message": "Email and password is not enabled" }
```

**Neon Console** → مشروعك → **Auth** → **Sign-in methods**
→ فعّل **Email & Password**

بدونه لا يستطيع أي طالب إنشاء حساب.

**وقرار يخصّك:** في نفس الصفحة خيار **Require email verification**.
- **مفعّل** = الطالب يؤكّد بريده قبل الدخول (أأمن، لكن يحتاج بريد جامعي يعمل)
- **معطّل** = دخول فوري، والموافقة تبقى يدوية منك عبر `status='pending'`

أرشّح **تعطيله** — عندك بالفعل نظام موافقة يدوية في `profiles.status`.

### 2. أضف النطاقات الموثوقة

**Neon Console** → **Auth** → **Trusted domains** — أضف:

```
http://localhost:8099
https://<اسم-موقعك>.pages.dev
```

بدونها يرفض الخادم الطلب بـ `MISSING_ORIGIN` **بدل** أن يقول إن كلمة المرور خاطئة — رسالة مربكة للطالب.

### 3. تأكّد أنك شغّلت ملفَّي SQL

إن لم تكن فعلت بعد:
`db/01_schema.sql` ← ثم ← `db/02_policies.sql` ← ثم **Refresh schema cache**

**اقرأ جدول التحقّق** في نهاية الملف الثاني: كل صف يجب أن يكون
`rls_on = true` و `policies > 0`.

---

## 🐛 ثلاثة أخطاء أمسكها الفحص الحيّ

هذه كانت ستكسر التطبيق صامتةً لو بنيت على الوثائق وحدها:

**1. مسار JWKS خاطئ**
كتبت `/api/auth/jwks` — الحقيقي `/.well-known/jwks.json`.
النتيجة كانت: **كل رفع صورة يفشل** بـ 401.

**2. الخوارزمية EdDSA لا RSA**
الـworker كان يتحقّق بـ`RSASSA-PKCS1-v1_5`. مشروعك يوقّع بـ**Ed25519**.
النتيجة: **رفض كل توكن صحيح**. أصلحته ليكتشف النوع تلقائياً.

**3. `callbackURL` يفسد تسجيل الدخول**
إرساله مع `/sign-in` يجعل الخادم يردّ `INVALID_CALLBACKURL` بدل التحقّق
من كلمة المرور. حذفته — تسجيل الدخول لا يعيد التوجيه أصلاً.

---

## ما بُني الآن

| الملف | الوظيفة |
|---|---|
| `config_sm.js` | ✅ رابطاك مضبوطان |
| `auth_sm.js` | تسجيل · دخول · خروج · توكن · رسائل خطأ بالفرنسية |
| `db_sm.js` | عميل Data API + استعلامات جاهزة |

**نقاط تصميمية:**

- **التوكن يُجدَّد تلقائياً** — أي 401 يُعيد المحاولة مرة واحدة بتوكن جديد
- **`credentials:'include'`** في كل نداء — الكوكي عابر للنطاقات، ونسيانه يعني أن الدخول "ينجح" ثم يصبح الطلب التالي مجهولاً
- **حارس base64** في `insert` و `update` — يرمي خطأً **قبل الشبكة** لو حاول أي كود كتابة `data:` في عمود رابط
- **`remove()` بلا فلتر مرفوضة** — لا حذف جدول كامل بالخطأ
- **الخصوصية من RLS لا من JavaScript** — `db_sm.js` يشكّل الاستعلامات فقط

الاختبارات: **296/296** (33 جديدة تضرب مشروعك الحيّ فعلياً).

---

## بعد ضبط الإعدادين

أخبرني وأكمل:
1. شاشة تسجيل الدخول/الحساب
2. ربط `useApi()` في كل وحدة بقاعدة البيانات الحقيقية
3. أول حساب أدمن لك
