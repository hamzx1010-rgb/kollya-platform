# خطة الإصلاح — اقرأها ووافق قبل أن أكتب سطراً

**أنت محق في كل نقطة.** فحصت الكود وأكّدت كل شكوى بالأرقام.
هذه ليست اعتذاراً — هذا ما وجدته بالضبط وما سأفعله.

---

## الاعتراف الأول: لماذا "لا شيء يُسجَّل"

فحصت الوحدات الاثنتي عشرة:

```
✗ feed_sm          لا يستورد db_sm إطلاقاً
✗ messages_sm      لا يستورد db_sm إطلاقاً
✗ stories_sm       لا يستورد db_sm إطلاقاً
✗ profile_sm       لا يستورد db_sm إطلاقاً
✗ campus_sm        لا يستورد db_sm إطلاقاً
✗ notifications_sm لا يستورد db_sm إطلاقاً
✗ hub_sm · leaderboard_sm · editor_sm · gif_sm · voice_sm
```

**صفر من اثنتي عشرة وحدة متصلة بقاعدة البيانات.**

كل ما تراه — المنشورات، المحادثات، الستوريز — بيانات مكتوبة داخل
الملفات نفسها. التعليق لا يُسجَّل لأنه لم يكن موصولاً بشيء من الأصل.

قلت لك سابقاً "الربط خطوتي التالية" ثم بنيت واجهات أكثر بدل أن أربط.
هذا خطأ في الترتيب، وهو سبب شعورك أن النسخة الجديدة أسوأ: **الأصلي
كان يكتب في Supabase فعلاً، والجديد لا يكتب في أي مكان.**

---

## البقّ المؤكَّد — بالسطر والرقم

### 1. الشريط المطوي يكسر كل شيء
```css
--nav-w-collapsed: 76px;
.nav-rail { padding: var(--s4) var(--s3); }   /* 12px يمين + 12px يسار */
.nav-item { padding: 0 var(--s3); }           /* 12px + 12px */
```
**76 − 24 − 24 = 28px** للأيقونة التي عرضها 22px. لا مجال للتنفّس،
فتنضغط الأيقونات وتختفي عملياً.

**الإصلاح:** عند الطي → حشو 8px، الأيقونة في المنتصف، ارتفاع ثابت 44px.

### 2. لوحة الرسائل تظهر عند نصف الشاشة فقط
```css
.dm { grid-template-columns: 340px minmax(0, 1fr); }
.dm.info-open { grid-template-columns: 340px minmax(0,1fr) 320px; }
```
340 + 320 = **660px محجوزة** قبل أن تبدأ المحادثة. مع شريط جانبي 248px
تحتاج شاشة أعرض من 1300px لترى شيئاً معقولاً.

**الإصلاح:** قائمة 300px · محادثة مرنة · لوحة الإنفو **تُغلق افتراضياً**
وتطفو فوق المحتوى تحت 1400px.

### 3. حلقات الستوريز مكسورة
```css
.story-ring > * { border: 2.5px solid var(--bg); width:100%; height:100% }
```
الابن `.av` عرضه 100% **زائد** حدود 2.5px **زائد** حشو الأب 2.5px →
يفيض خارج الدائرة. `box-sizing` لا يُطبَّق لأن العرض محسوب مسبقاً.

**الإصلاح:** حساب صريح `calc(100% - 5px)` مع توسيط.

### 4. الستوريز "Chargement" ولا شيء يظهر
`openStories()` يقرأ من `SAMPLE` ثابت، والصور روابط Unsplash خارجية.
إن حجبها المتصفح أو الشبكة → إطار أسود فارغ بلا رسالة خطأ.

### 5. سبع ميزات تقول "قريباً"
```
Création de canal · Création de story · Export
Recherche dans la conversation · Repartage · Transfert
```
**سأنفّذها كلها أو أحذف أزرارها.** زر لا يفعل شيئاً أسوأ من غيابه.

### 6. الصورة الشخصية والغلاف
يعملان محلياً بـ`URL.createObjectURL` فقط — يختفيان عند التحديث لأن
R2 غير مُعدّ ولا شيء يُرفع.

---

## ما سأفعله — بالترتيب

### المرحلة 1 · الربط بقاعدة البيانات ← الأهم بلا منازع
أستبدل كل بيانات `SAMPLE` باستدعاءات Neon حقيقية:

| الوحدة | يُسجَّل في |
|---|---|
| المنشورات · الإعجاب · التعليق · الاستطلاع | `posts` · `post_likes` · `comments` |
| الرسائل | `messages` مع RLS |
| الستوريز | `stories` |
| البروفايل · المتابعة | `profiles` · `follows` |
| القنوات · الفعاليات · Q&A | جداولها |
| الإشعارات | `notifications` |
| XP · الشارات | `profiles.xp` |

**معيار النجاح:** تكتب تعليقاً → تحدّث الصفحة → **التعليق ما زال هناك**.
وتراه في Neon SQL Editor.

### المرحلة 2 · إصلاح التخطيط
- الشريط المطوي: أيقونات واضحة، بلا انهيار
- الرسائل: تعمل من 1024px فأعلى، والإنفو تطفو لا تزاحم
- حلقات الستوريز: هندسة صحيحة

### المرحلة 3 · شكل الأصلي
- **الفعاليات و Q&A:** hero علوي متدرّج بحواف 28px وزر إنشاء بارز — كالأصلي
- **البروفايل:** إعادة تصميم كامل، ليس "رخيصاً"
- **المحادثات:** تخطيط الأصلي المريح

### المرحلة 4 · الميزات الناقصة
إنشاء ستوري · إنشاء قناة · البحث في المحادثة · إعادة التوجيه · المشاركة
— **أو أحذف الأزرار.** لا "قريباً".

### المرحلة 5 · الصور والفيديو (تحتاج R2)
رفع حقيقي للصورة الشخصية والغلاف والمنشورات والفيديو.
**هذه وحدها تحتاج منك مفاتيح R2.**

---

## ما أحتاجه منك

### الآن — لا شيء
المراحل 1 و2 و3 و4 لا تحتاج أي شيء منك. أبدأ فوراً.

### للمرحلة 5 فقط
```
R2_PUBLIC_URL   = https://pub-xxxx.r2.dev
UPLOAD_URL      = https://xxx.workers.dev
```
(المفاتيح السرية تبقى عندك عبر `wrangler secret put`)

### وأكّد لي
هل شغّلت `db/01_schema.sql` و `02_policies.sql` على Neon؟
إن لم يكن — الربط سيفشل، ويجب تشغيلهما أولاً.

---

## سؤال واحد يحدد الترتيب

**ما الأهم عندك الآن؟**

**أ) الربط بقاعدة البيانات أولاً** — كل شيء يُسجَّل ويبقى بعد التحديث،
حتى لو بقي الشكل كما هو أسبوعاً. ← **رأيي**

**ب) الشكل أولاً** — الفعاليات و Q&A والبروفايل والتخطيط، ثم الربط.

**ج) الاثنان معاً، وحدة وحدة** — أربط الفيد وأصلح شكله، ثم الرسائل،
ثم الستوريز… أبطأ لكن كل جزء يكتمل تماماً قبل التالي.

---

## تعهّد

**لن أعرض عليك شيئاً وأقول إنه يعمل قبل أن أتحقق أنه يكتب في Neon فعلاً.**

اختباراتي كانت تمر لأنها تفحص المنطق في متصفح وهمي — لا لأن التطبيق
يعمل في متصفحك. سأضيف اختبارات تكتب في قاعدة بياناتك الحقيقية وتقرأ
مرة أخرى للتأكد.

**قل لي (أ) أو (ب) أو (ج) وأبدأ.**
