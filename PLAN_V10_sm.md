# PLAN V10 — سبب عدم وصول الرسائل، وبناء Koliya 1.00

---

## 1. كنت محقّاً في الأمرين. إليك الدليل.

### أ) نسخة 1.2 لا تحتوي على تغييرات V9 إطلاقاً

فحصت الـ APK الذي أعطيتك إياه:

```
hub-strip في CSS داخل الـ APK        : 0
campus-tabs في CSS داخل الـ APK      : 0
nav-desk في CSS داخل الـ APK         : 0
data-nav="campus" في HTML داخل الـ APK: 0
progressStripMarkup في feed داخل APK : 0
```

**صفر في كل شيء.** عدّلت `public/` وصوّرت من Chrome، ولم أُعِد بناء الـ APK
أبداً. الصور كانت حقيقية لكنها لا تمثّل الملف الذي بيدك. هذا بالضبط
السلوك الذي طلبت منّي ألّا أفعله. أنا مخطئ ولا عذر لي.

### ب) سبب عدم وصول الرسائل — ليس في التطبيق، بل في قاعدة البيانات

كان حدسك صحيحاً: «ظهرت أنها في كيفية وصول الرسائل».

`api_sm.js` سطر 359 — دالة إرسال الرسالة:
```js
async sendMessage(payload) {
  ...
  const [created] = await db.insert('messages', row);   // ← تُدرج في messages
  return shapeMessage(created);                          // ← وتنتهي
}
```

**لا تكتب أي صف في جدول `notifications`.**

وفحصت المُشغّلات (triggers) في المخطّط كاملاً — ثلاثة فقط:
```
trg_guard_profile_progress  → profiles
trg_track_name_change       → profiles
trg_route_new_message       → messages  (يوجّه الغرباء إلى dm_requests فقط)
```
وبحثت عن أي `INSERT INTO notifications` في المخطّط: **لا يوجد ولا واحد.**

#### السلسلة كاملة، ولماذا تظهر لك الرسالة فقط داخل المحادثة

```
صديقك يرسل رسالة
   ↓
INSERT INTO messages          ✅ تُحفظ
INSERT INTO notifications     ❌ لا يحدث أبداً
   ↓
pending_alerts() تقرأ من notifications فقط  →  ترجع لا شيء
   ↓
خدمة Java في الخلفية تستطلع pending_alerts  →  لا شيء  →  لا إشعار
notify_sm.js يستطلع pending_alerts          →  لا شيء  →  لا إشعار
   ↓
الشيء الوحيد الذي يرى الرسالة هو messages_sm.js
لأنه يستطلع جدول messages مباشرة —
وهو يعمل فقط عندما تكون الصفحة مفتوحة.
```

لهذا تحديداً: **يجب أن تكون داخل صفحة الرسائل، وداخل المحادثة**. لا علاقة
للأمر بأندرويد ولا بالخدمة الخلفية التي بنيتها — كانت تستطلع جدولاً فارغاً.

فبنيتُ في V8 آلة استطلاع كاملة تسأل سؤالاً لا أحد يكتب جوابه. الجزء
الأندرويدي سليم؛ المصدر كان فارغاً.

---

## 2. الإصلاح

### أ) مُشغّل قاعدة بيانات يكتب الإشعار — `AFTER INSERT ON messages`

المكان الصحيح هو قاعدة البيانات وليس المتصفّح:
- يعمل مهما كان المُرسِل (ويب · أندرويد · نسخة قديمة من التطبيق)
- لا يمكن تفويته إذا أُغلق المتصفّح لحظة الإرسال
- `SECURITY DEFINER` يتجاوز RLS، لأن المُرسِل لا يملك صلاحية الكتابة في
  صندوق إشعارات المُستقبِل — وهذا صحيح أمنياً

المنطق:
```
AFTER INSERT ON messages:
  إذا كان المُستقبِل قد كتم هذه المحادثة → لا شيء
  إذا كانت أول رسالة من غريب (dm_requests) → kind = 'dm_request'
  غير ذلك → kind = 'message'
  النص: أول 120 حرفاً، أو «صورة»/«رسالة صوتية»/«ملف» حسب النوع
```

وأضيف **تجميعاً**: إذا وُجد إشعار غير مقروء من نفس المُرسِل خلال آخر
دقيقتين، أُحدّثه بدل إنشاء صف جديد — وإلا فإن عشرين رسالة متتالية تعني
عشرين اهتزازاً.

سأضيفه في ملف ترحيل جديد `12_dm_notify_sm.sql` **وأدمجه في
`FULL_SCHEMA_sm.sql`** حتى يبقى الملف الواحد الذي طلبته كافياً وحده.

### ب) تنظيف عند القراءة
عند فتح المحادثة تُعلَّم إشعارات ذلك المُرسِل كمقروءة، وإلا بقيت
`pending_alerts` تُعيدها إلى الأبد.

### ج) بناء **Koliya-1.00.apk** فعلياً
- بلا أرقام إضافية كما طلبت: اسم الملف `Koliya-1.00.apk`
- `versionName = "1.00"` و `versionCode = 5` (يجب أن يبقى يرتفع وإلا عاد
  فخّ «يظهر كتحديث»)
- **مشكلة يجب أن أحلّها أولاً:** ملفات جافا الـ16 المصدرية ضاعت مجدداً في
  استرجاع اللقطة. الكود مُترجَم وسليم داخل `Koliya-1.2.apk`، فسأستخرجه
  وأعيد بناء المشروع قبل البناء.

### د) وأخيراً — التحقّق الذي كان يجب أن أفعله آخر مرة
بعد البناء **أفكّ الـ APK الناتج** وأتأكّد بالأرقام أن بداخله:
`hub-strip` · `campus-tabs` · `data-nav="campus"` · `progressStripMarkup`
· أصناف الخدمة الخلفية. ولن أقول «تمّ» قبل أن أرى الأرقام من الملف نفسه.

---

## 3. ما لن أدّعيه
- هذا **استطلاع** وليس push حقيقياً؛ التأخير 20 ثانية (15 أثناء الشحن).
- لا أستطيع تشغيل الـ APK: لا `/dev/kvm` و2 GB ذاكرة. سأتحقّق بنيوياً فقط
  وأقول لك بوضوح ما لم أقِسه.
- **يجب أن تشغّل `db/FULL_SCHEMA_sm.sql` من جديد في Neon** ثم
  Data API → Refresh schema cache. بدونها لن يتغيّر شيء مهما ثبّتّ من APK،
  لأن الإصلاح الحقيقي في قاعدة البيانات.
