# PLAN V11 — الإشعارات كما في إنستغرام، والرسائل تظهر فوراً
**قبل التنفيذ.**

---

## أنت محقّ، وأنا فهمت الآن أين المشكل

بنيتُ في المرات السابقة خدمة خلفية في جافا، وأصلحت المُشغّل في قاعدة
البيانات — وكلاهما صحيح لكنه **ليس ما اشتكيت منه**. المشكل الذي تصفه في
جافاسكريبت، في المتصفّح، وموجود منذ البداية.

هذه ثلاثة أعطال منفصلة قِستها في الكود، لا تخميناً:

---

## العطل 1 — الاستطلاع لا يعمل إلا وأنت داخل محادثة

`messages_sm.js` سطر 135:

```js
async function beat(force = false) {
  if (pollBusy || !peer || !api?.listMessages) return;   // ← هنا
```

`peer` هو الشخص الذي تحادثه الآن. إن لم تكن داخل محادثة فـ `peer = null`
و **`beat()` تخرج فوراً دون أن تسأل الخادم عن أي شيء**.

وأسوأ: `startPolling()` تُستدعى من داخل `openThread()` فقط (سطر 2110)،
و `teardownMessages()` توقفها عند مغادرة الصفحة. يعني:

| أين أنت | هل يستطلع؟ |
|---|---|
| داخل المحادثة | ✅ كل 1.5 ث |
| في قائمة الرسائل، بلا محادثة مفتوحة | ❌ `peer` فارغ |
| في الصفحة الرئيسية / أي صفحة أخرى | ❌ المؤقّت متوقّف أصلاً |

**هذا بالضبط ما وصفته**: «يجب أن أكون داخل صفحة الرسائل، وداخل المحادثة».

### الحل: مستطلِع صندوق وارد على مستوى التطبيق
مستطلِع ثانٍ **مستقل عن الصفحة**، يبدأ عند تسجيل الدخول ولا يتوقّف:
- يسأل عن الرسائل الجديدة **لكل المحادثات**، لا للمفتوحة فقط
- يعمل من أي صفحة: الرئيسية، الملف الشخصي، الفعاليات…
- كل 5 ث والتطبيق مرئي · 20 ث وهو مخفي
- عند وصول رسالة: يحدّث الشارة، ويحدّث قائمة المحادثات **إن كانت معروضة**،
  ويطلق `dm:incoming` ليقرّر الإشعار
- استطلاع المحادثة المفتوحة (1.5 ث) يبقى كما هو للحوار الحيّ

**النتيجة:** الرسالة تظهر في القائمة وأنت في الصفحة الرئيسية، بلا إعادة
تحميل ولا ضغط على «الرسائل» مرتين.

---

## العطل 2 — الإشعار لا يظهر إلا إذا كنت تراه أصلاً (عكس المطلوب تماماً)

`notify_sm.js` سطر 280:

```js
async function checkAlerts() {
  if (!canNotify() || !me.id) return;
  if (!document.hidden && location.hash.startsWith('#/notifications')) return;
```

هذا يبدو معقولاً، لكن العطل الحقيقي في `dm:incoming`: الحدث **يُطلق من
داخل `beat()`** — أي من نفس الدالة التي لا تعمل إلا والمحادثة مفتوحة.
فالإشعار مربوط بالشيء الذي يفترض أن يعوّضه.

وهناك ثغرة ثانية: عند وجود إشعار، الشرط الوحيد للصمت هو أن تكون في
`#/notifications`. أما إن كنت **داخل المحادثة نفسها** فالمنطق موجود في
`initNotify` لكن `checkAlerts` لا تعرفه.

### الحل: منطق إنستغرام بالضبط
| الحالة | السلوك |
|---|---|
| داخل نفس المحادثة | **لا إشعار** — أنت تراها |
| في التطبيق، صفحة أخرى | إشعار داخلي (toast) + شارة |
| التطبيق مخفي / مغلق | إشعار نظام |
| صفحة الإشعارات مفتوحة | تحديث القائمة فقط |

وينطبق على **follow · like · comment · mention** لا الرسائل وحدها.

---

## العطل 3 — «أزل القديم الذي يبقيها نشطة دائماً»

فهمت هذا على أنه الإشعار الدائم `IMPORTANCE_MIN` («كلية تستقبل الرسائل»)
الذي بنيته في V8 — الذي يفرضه أندرويد على أي `foreground service`.

إنستغرام لا يعرض شيئاً كهذا لأنه يستعمل **FCM**، فلا يحتاج خدمة أمامية.

**اقتراحي:** أجعل الافتراضي **إيقاف** الخدمة الأمامية، والاعتماد على:
- المستطلِع داخل التطبيق (العطل 1) — يغطّي «التطبيق مفتوح»
- `SyncWorker` كل 15 دقيقة — يغطّي «مغلق»، بلا أي إشعار دائم
- ومفتاح في الإعدادات لمن يريد الوصول الفوري ويقبل الإشعار الدائم

هذا يزيل الإزعاج ويُبقي الخيار. **لكن هذا قرارك** — السؤال في الأسفل.

---

## العطل 4 — الأرقام تختفي عند الصفر

`feed_sm.js` سطر 180:
```js
<span class="c">${p.likes.length || ''}</span>
```
`0 || ''` = **سلسلة فارغة**. فمنشور بلا إعجاب لا يعرض `0` بل لا شيء —
فيبدو الزرّ «ثابتاً ولا يفعل شيئاً» لأنك تضغط ولا ترى رقماً يتغيّر من
لا-شيء إلى `1`.

والأزرار **تعمل فعلاً** (تحقّقت: `runPostAction` موجودة سطر 822 وتُستدعى)،
لكن انعدام الرقم يجعلها تبدو ميّتة.

وزرّ **المشاركة بلا عدّاد إطلاقاً** (سطر 185) ولا يوجد `shares` في المخطّط.

### الحل
- إظهار `0` دائماً بدل الفراغ، بأرقام مضغوطة (`1.2k`)
- تحديث الرقم فوراً عند الضغط (تفاؤلي) والتراجع عند فشل الشبكة
- عدّاد حقيقي للمشاركة: جدول `post_shares` + زيادته عند المشاركة/النسخ

---

## ما سأسلّمه
```
public/js/core/inbox_sm.js       ← جديد: مستطلِع الوارد على مستوى التطبيق
public/js/core/notify_sm.js      ← منطق إنستغرام
public/js/features/messages_sm.js← يستهلك المستطلِع بدل أن يملك واحداً
public/js/features/feed_sm.js    ← الأرقام تظهر وتتحرّك
db/13_shares_sm.sql              ← جدول المشاركات + دمج في FULL_SCHEMA
tests/browser/inbox.test.mjs     ← جديد
apk/Koliya-1.00.apk              ← إعادة بناء (versionCode يرتفع)
```

## التحقّق
- اختبار Chrome حقيقي **بمحاكاة الحالة التي تشتكي منها**: أفتح الصفحة
  الرئيسية، أُدخل رسالة في الخادم الوهمي، وأتأكّد أنها ظهرت **دون فتح
  صفحة الرسائل**
- الأرقام: أضغط إعجاب وأقرأ الرقم قبل وبعد
- الصور **من الملفات المفكوكة من الـ APK**، لا من `public/`
- 851 + 245 + 34 + فحوص محتوى الـ APK

## لن أدّعي
- ليس push حقيقياً. الفوري داخل التطبيق فقط؛ خارجه 15 دقيقة إن أطفأنا
  الخدمة الأمامية.
- **لا أستطيع تشغيل الـ APK** — لا `/dev/kvm`.
- **يجب أن تشغّل `FULL_SCHEMA_sm.sql` من جديد** (مُشغّل الرسائل من V10 +
  جدول المشاركات).
