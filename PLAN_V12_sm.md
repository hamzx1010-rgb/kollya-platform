# PLAN V12 — من الأهم إلى الأسهل
**قبل التنفيذ. اقرأ ووافق أو غيّر الترتيب.**

---

## ملاحظتك هي التشخيص نفسه

قلت: «إشعارات السلسلة تعمل والتطبيق مغلق، إلا الرسائل».

هذه **أهم معلومة أعطيتني إياها**، لأنها تحصر العطل في مكان واحد بالضبط.
انظر الفرق بين الاثنين:

```
ReminderWorker (السلسلة — يعمل ✅)
   WorkManager يوقظه → ينشئ إشعاراً محلياً → انتهى.
   لا شبكة · لا كوكي · لا مصادقة · لا قاعدة بيانات.

SyncService.tick (الرسائل — لا يعمل ❌)
   1. CookieManager.getCookie(AUTH_URL)      ← يقرأ جلسة الـ WebView
   2. GET  {AUTH}/token                       ← يحوّلها إلى JWT
   3. POST {DATA_API}/rpc/pending_alerts      ← يسأل عن الجديد
   4. ينشئ الإشعار
```

المسار الثاني فيه **أربع خطوات قد تفشل**، والأولى أرجحها: `getCookie()`
تُرجع `null` لأن كوكي الجلسة `httpOnly` + `SameSite`، وهناك علّة معروفة
في WebView تمنع تسليمه لجافا. وحين تفشل، `NeonClient` يُرجع
`NOT_SIGNED_IN` **بصمت** — لا إشعار، ولا رسالة خطأ، ولا شيء تراه.

هذا يفسّر بدقة لماذا السلسلة تصل والرسائل لا تصل. **ليست مشكلة جدولة.**

### الإصلاح — لا نعتمد على الكوكي إطلاقاً
حين تنجح المصادقة في الصفحة، أحفظ الـ JWT + توقيت انتهائه في
`SharedPreferences` عبر جسر `AndroidSync`. تقرأه جافا مباشرة، فتسقط
الخطوتان 1 و2 كلياً. وإن انتهى، تُجدّده بـ refresh token محفوظ بنفس
الطريقة.

**وأضيف تشخيصاً مرئياً**: بطاقة الإعدادات تعرض «آخر تحقّق · النتيجة».
فلو فشل مجدداً سترى `NOT_SIGNED_IN` أو `HTTP_401` بدل الصمت. لا أريدك أن
تكتشف عطلاً صامتاً مرة أخرى.

---

## الترتيب المقترح

### 1 — الرسائل تصل والتطبيق مغلق (الأهم)
كما أعلاه: JWT مُخزّن بدل الكوكي · تشخيص مرئي · واختبار يزيّف
`AndroidSync` بلا كوكي ويتأكّد أن الـ JWT المُخزّن يُستعمل.

### 2 — اللافتة البيضاء تصبح إشعار نظام حقيقياً
اللافتة الداخلية التي بنيتها في V11 تعمل داخل التطبيق فقط. سأربطها بـ
`AndroidNotify.show()` فتصير إشعاراً حقيقياً في الشريط عند وصول رسالة،
حتى والتطبيق في الخلفية مباشرة.

### 3 — إشعارات المتابعة/الإعجاب/الإشارة
الكود **موجود**: `api_sm.js:677` يُدرج الصف، وصفحة الإشعارات تعرض
`follow · like · mention · request`. وسياسة RLS تسمح
(`actor_id = auth.user_id()`).
⇒ **إذاً العطل في مكان آخر**، وأرجّح `is_approved()` في نفس السياسة، أو أن
الإدراج يفشل و`.catch(() => {})` يبتلع الخطأ بصمت.
**لن أخمّن**: سأعيد إنتاجه في PostgreSQL حقيقي كطالب عبر دور
`authenticated`، وأقول لك السبب بالأرقام قبل أن أصلح. وسأزيل `.catch()`
الصامت على أي حال.

### 4 — بطاقة الطالب ظاهرة للجميع
`student_card` غير معروض في `profile_sm.js` إطلاقاً. سأضيفه في الترويسة
كشارة (`CS-042`). سؤال في الأسفل: للجميع أم للأصدقاء فقط؟

### 5 — إزالة الفعاليات/الأسئلة من Explore
أنا من أضافها في V9، وأنت تريدها في «الحرم» فقط. Explore يعود للبحث وحده.
تغيير صغير ومباشر.

### 6 — زرّ + في الرسائل (مجموعات)
الزرّ **موصول فعلاً** بـ `openNewConversation` (سطر 2213)، أي أنه يفتح
محادثة فردية لا مجموعة. سأحوّله إلى قائمة: «رسالة جديدة» / «مجموعة جديدة».
يحتاج جداول جديدة (`groups`, `group_members`).

### 7 — القنوات كمجموعات على طريقة تيليغرام
ضغط قناة يفتح محادثة جماعية · المشرف يعيّن مشرفين آخرين · وضع «المشرفون
فقط يكتبون». يحتاج `channel_members` بأدوار + سياسات RLS للكتابة.

### 8 — الفعالية = محادثة جماعية تلقائية + صورة
الانضمام للفعالية يُدخلك مجموعتها · ورفع صورة تظهر بجانب المعلومات
(يمين/يسار باختيارك).

---

## تقديري للحجم — وسبب سؤالي

البنود 6 و7 و8 ليست تعديلات واجهة. المحادثات الجماعية ميزة كاملة:
**5 جداول جديدة على الأقل**، سياسات RLS للأدوار، شاشة مجموعة، إدارة
أعضاء، ودمج مع الرسائل الموجودة. وحدها تعادل حجم كل ما بُني في V8–V11
مجتمعاً، ولن تدخل في جولة واحدة بجودة تُختبر.

البنود 1–5 كلها **إصلاحات** لأشياء موجودة ومكسورة، وأستطيع إنهاءها
واختبارها وتسليم APK مُتحقَّق منه في هذه الجولة.

---

## ما لن أدّعيه
- **لم أشغّل APK ولا مرة** — لا `/dev/kvm`. الفحص بنيوي + Chrome حقيقي.
- البند 1 إصلاح **مبني على تشخيص**، لا على قياس على هاتفك. لهذا أضيف
  التشخيص المرئي: إن بقي معطلاً سترى السبب بدل الصمت.
- الوصول الفوري والتطبيق مغلق تماماً يحتاج FCM؛ الحالي استطلاع.
