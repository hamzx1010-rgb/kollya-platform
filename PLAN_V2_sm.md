# الخطة الموحّدة — كل الأخطاء، القديمة والجديدة

**لا تعديل واحد قبل موافقتك.** كل سطر أدناه مثبت بالأرقام من كودك، لا من ذاكرتي.

---

# الجزء الأول · ما وجدته

## ١. سطر واحد عطّل 44 موضع رسم  ← الأخطر

```js
// public/js/core/utils_sm.js — السطر 356
export function safeUrl(url) {
  const s = String(url ?? '').trim();
  return /^(https?:|blob:|\/)/i.test(s) ? s : '';   // ← data: غير مذكورة
}
```

```
safeUrl('https://x.com/a.jpg')          →  "https://x.com/a.jpg"   ✅
safeUrl('data:image/jpeg;base64,/9j/')  →  ""                      ❌ فارغة
```

في الجلسة الماضية نقلتُ الوسائط إلى Postgres كـ`data:` URL،
**ثم تركتُ الدالة التي ترسمها تحجب `data:` بالضبط.**

عددتُ المواضع:

```
messages_sm.js      13   ← صور المحادثات
profile_sm.js       11   ← الصورة الشخصية والغلاف
feed_sm.js           8   ← صور المنشورات
stories_sm.js        3   ← صور الستوري
campus_sm.js         3
gif_sm.js            2
notifications_sm.js  1
                    ──
                    44
```

**يفسّر:** «الستوري لا تُظهر الصور» · «الصورة والغلاف لا يعملان» · صور الرسائل.

---

## ٢. زر الحذف خلف الستوري ← وأنت محق، الخطأ منهجي

بحثتُ عن نظائره كما طلبت. جدول z-index كامل:

```
.sv          عارض الستوري      500   ← الغطاء
──────────────────────────────────────────────
.toast       التأكيدات         400   مدفون
.menu        قائمة يمين        300   مدفون
.ctx-menu    القائمة السياقية  300   مدفون
.modal       قائمة المشاهدين   201   مدفون
.scrim       خلفية النافذة     200   مدفون
.lightbox    تكبير الصورة      500   تعادل → ترتيب DOM يقرّر
```

فليس زر الحذف وحده: **تأكيد الحذف، وقائمة المشاهدين، وكل toast، وكل
قائمة يمين** كلها محبوسة تحت الستوري. ونفس المشكلة تصيب `lightbox`
لأنه يتعادل مع `.sv` فيقرّر ترتيب DOM عشوائياً.

**الإصلاح:** طبقات صريحة — `.sv` = 450، والنوافذ والقوائم والـtoast فوقها.

---

## ٣. «Nouveau message» لا يُظهر شيئاً

```js
const refresh = debounce(async () => { … }, 200);   // مؤجَّلة 200ms
list.innerHTML = skeletonList(4, 'conv');
const dlg = modal({ … });
refresh();                    // ← تُجدول، لا تُنفَّذ
```

ومع ذلك:

```js
const people = await api.contacts(...).catch(() => []);
                                        ↑ يبتلع الخطأ صامتاً
```

فإن فشل الاستعلام — أو أعاد صفراً لأن `status='approved'` لا يطابق أحداً —
ترى «Aucun étudiant trouvé» أو الهيكل العظمي إلى الأبد. **لا رسالة خطأ.**

**الإصلاح:** تحميل فوري بلا تأجيل + إظهار الخطأ الحقيقي + زر إعادة.

---

## ٤. العدّاد لا يستطيع العدّ

```js
// db_sm.js:144
fetch(`/${table}${qs({ select: 'id', ...params })}`, { method:'HEAD' })
                        ↑ مفروضة دائماً
```

جداول الوصل **بلا عمود `id`** — مفتاحها مركّب:

```
follows           PRIMARY KEY (follower_id, followee_id)   لا id
post_likes        PRIMARY KEY (post_id, user_id)           لا id
post_saves                                                 لا id
event_attendees                                            لا id
```

و`api_sm.js` يستدعي `db.count()` على هذه الأربعة بالذات، ثم
`.catch(() => 0)` يبتلع الخطأ 400.

**عدّاد المتابعين صفر لأن السؤال خاطئ، لا لأن المتابعة تفشل.**

---

## ٥. الإشعارات: شكلان متصادمان

```js
// notifications_sm.js — كائن
setState({ unread: { ...state.unread, notifications: n } });

// app_sm.js (كتبتُها الجلسة الماضية) — رقم
setState({ unread: n });
```

`shell_sm.js` يقرأ `state.unread.messages + state.unread.notifications`
→ `undefined + undefined` → **NaN**. الجرس لا يعمل.

---

## ٦. الحساب الخاص يحمي نصف الأشياء

```sql
can_view()        ← يفحص is_private  ✅
posts_read        ← يستدعي can_view()  ✅
stories_read      ← لا يستدعيه         ❌ ستورياتك مكشوفة
profiles_read     ← لا يستدعيه         ❌ بروفايلك مكشوف
```

---

## ٧. الرسائل ليست فورية

أصلك استخدم **Supabase Realtime**:

```js
// ملفك، السطر 5558
.on('postgres_changes', { event:'INSERT', table:'messages' }, …)
.subscribe()
```

**Neon Data API بلا WebSocket إطلاقاً.** وضعتُ `setInterval` كل 5 ثوانٍ
وسمّيته فورياً. لم يكن، ولاحظتَ.

---

# الجزء الثاني · ما سأفعله

## المرحلة ١ — الإصلاحات الحاسمة

| # | الإصلاح | الملف |
|---|---|---|
| 1 | `safeUrl()` تقبل `data:image/video/audio`، وتبقى ترفض `javascript:` | `utils_sm.js` |
| 2 | سلّم z-index صريح: `.sv` 450 · modal 600 · menu 700 · toast 800 | `base_sm.css` · `layout_sm.css` |
| 3 | `db.count()` يستخدم مفتاح كل جدول الصحيح | `db_sm.js` |
| 4 | توحيد `unread` ككائن في كل مكان | `notifications_sm.js` · `app_sm.js` |
| 5 | `stories_read` و`profiles_read` يستدعيان `can_view()` | `db/07_privacy_sm.sql` 🆕 |
| 6 | حفظ البروفايل يعيد القراءة ويطبع الخطأ بدل ابتلاعه | `api_sm.js` · `profile_sm.js` |

## المرحلة ٢ — الرسائل، بمنطق ملفك الأصلي

**«Nouveau message»:**
تحميل فوري · بحث حيّ · الأخيرون أولاً · خطأ مرئي · زر إعادة.

**زر مراسلة حيثما يفيد** — كما طلبت:
```
بطاقة البروفايل      للحسابات العامة والذين تتابعهم
«طلاب لتكتشفهم»      زر مراسلة بجانب «Voir»
نتائج البحث          نفس الشيء
```
الحسابات الخاصة التي لا تتابعها: الزر **مخفي**، لا معطّل بلا سبب.

**المجلّدات — من ملفك الأصلي بالضبط:**
```js
// ملفك: ['all','pinned','unread','study','muted']  + archived + requests
```
سأنقل نفس المجموعة، مع الثيم الجديد:
`Tous · Épinglés · Non lus · Études · Muets · Archivés`
تُخزَّن في `chat_folders` (جدول جديد) لتتبعك بين الأجهزة —
أصلك خزّنها في `localStorage` فكانت تضيع.

**التحديث القريب من الفوري:**
```
إرسال            → الفقاعة فوراً، ثم تُثبَّت من القاعدة
محادثة مفتوحة    → استعلام كل 1.5s بفلتر created_at > آخر رسالة
                   (طلب صغير، لا يعيد تحميل المحادثة)
النافذة مخفيّة   → 15s
تبويباتك         → BroadcastChannel، تتزامن بلا أي طلب
```

**سأختبر الطرفين فعلاً كما طلبت:** مُرسِل ومُستقبِل، صورة وصوت وملف،
تفاعل، ردّ، تعديل، حذف، علامة قراءة، «يكتب الآن».

## المرحلة ٣ — اختبار حقيقي، لا jsdom فقط

هذا سبب مرور 454 اختباراً وتطبيقك مكسور:

> **لا اختبار واحد سأل: «هل تظهر صورة `data:` فعلاً؟»**
> كلها تفحص المنطق، لا البكسل. `safeUrl()` مرّت في كلها.

سأضيف:
- اختبار يمرّر `data:` عبر كل مسار رسم ويؤكد أن `src` ليس فارغاً
- اختبار z-index يقارن الطبقات رقمياً
- سيناريو محادثة من طرفين في نفس الاختبار

---

## ما لن أفعله

- **لن أعيد كتابة `messages_sm.js` من الصفر.** أصلح الموجود.
- **لن أسمّيه «فورياً»** وهو 1.5 ثانية. سأسمّيه باسمه.
- **لن أقول «يعمل»** قبل أن أريك صفاً في Neon.

---

## الترتيب

```
١  safeUrl        ← يصلح 44 موضعاً بسطر واحد
٢  z-index        ← زر الحذف وكل نظائره
٣  count + unread ← العدّادات والجرس
٤  الخصوصية SQL
٥  Nouveau message + أزرار المراسلة
٦  المجلّدات
٧  التحديث القريب من الفوري + اختبار الطرفين
```

**قل «ابدأ» فأنفّذ بهذا الترتيب.**
إن أردت تغيير الترتيب أو حذف شيء، قل الآن.
