# خطة V3 — اقرأها ووافق قبل أن أكتب سطراً

ثماني نقاط. فحصتُ كلاً منها في الكود، ووجدتُ **أربعة أخطاء جذرية**
لم تكن ظاهرة من الواجهة — ثلاثة منها في قاعدة البيانات.

---

# الجزء الأول · ما وجدته

## ١. المحادثة تنزل خارج الشاشة ← وأنت محق تماماً

سلسلة الارتفاع مكسورة في حلقة واحدة:

```
.main        height: 100dvh   overflow: hidden      ✅ ارتفاع حقيقي
.view        flex: 1          overflow-y: auto      ✅ يملأ .main
.view-inner  (لا ارتفاع)      (لا min-height)       ❌ هنا تنكسر
.dm          height: 100%     →  100% من auto = auto
.thread      height: 100%     →  auto
.thread-body flex:1 min-h:0   →  لا شيء ليملأه
```

`.thread-body` لا يحصل على صندوق محدود أبداً، **فينمو مع كل رسالة**.
الصفحة كلها تتمدّد بدل أن تنزلق المحادثة داخل نفسها. النتيجة:
مربّع الكتابة يُدفَع تحت حافة الشاشة، وأحدث رسالة تخرج من المنظر.

**والأسوأ:** الكود صحيح فعلاً —

```js
scrollToBottom()  ← موجودة، وتُستدعى في 5 مواضع
```

لكنها تحرّك `#threadBody` الذي **لا شريط تمرير له**، لأنه غير محدود.
فالجافاسكربت سليم وCSS يهزمه.

**وملفك الأصلي كان يفعلها صح:**

```css
.msg-l { height: calc(100vh - 52px); overflow: hidden; min-height: 0 }
```

ارتفاع صريح، لا `height:100%` عبر أب بلا ارتفاع. هذا بالضبط ما ينقصني.

**الإصلاح:**
- `.view-inner` يرث ارتفاعاً حقيقياً حين تكون الشاشة ملء العرض
- `.dm` بارتفاع صريح `calc(100dvh - var(--topbar-h))` كأصلك
- عند الفتح: القفز إلى **الأحدث فوراً وبلا حركة** — لا أحد يريد
  مشاهدة تمرير عبر 200 رسالة
- رسالة جديدة وأنت في الأسفل → تتبعها بنعومة
- رسالة جديدة وأنت تقرأ الأعلى → **لا تُزحزَح**، ويظهر زر «↓ رسالة جديدة»
- إرسالك أنت → دائماً إلى الأسفل

## ٢. زر «Enregistrer» يقول «لم يتغيّر شيء» ← خطأ سببتُه

`db/06_game_sm.sql` دهس سياسة التحديث الصحيحة:

```sql
-- 02_policies.sql (الأصل، سليم)
WITH CHECK (
  id = auth.user_id()
  AND role   = (SELECT role   FROM profiles WHERE id = auth.user_id())
  AND status = (SELECT status FROM profiles WHERE id = auth.user_id())
)

-- 06_game_sm.sql (كتبتُها أنا، وكسرت الحفظ)
WITH CHECK (id = auth.user_id() AND role = 'student')
                                    ↑ أي حساب دوره 'admin'
                                      لا يستطيع حفظ بروفايله إطلاقاً
```

**والرسالة تكذب فوق ذلك:**

```js
: 'Enregistrement échoué — rien n\'a été modifié.'   // احتياطية، لأي خطأ
```

تظهر مهما كان السبب — انتهاء جلسة، رفض RLS، عمود مفقود — فتقول
«لم يتغيّر شيء» بينما غيّرتَ فعلاً.

## ٣. التحدّيات لا تُسجَّل ← أخطر ما وجدت

```sql
ALTER TABLE quests    FORCE ROW LEVEL SECURITY;
ALTER TABLE xp_events FORCE ROW LEVEL SECURITY;
```

`FORCE` يعني: **RLS تُطبَّق حتى على مالك الجدول** — وهو بالضبط من
تعمل باسمه دوال `SECURITY DEFINER`.

والسياسات الموجودة:

```
xp_read_own      ON xp_events  FOR SELECT   ← قراءة فقط
quests_read_own  ON quests     FOR SELECT   ← قراءة فقط
```

**لا سياسة INSERT ولا UPDATE إطلاقاً.** فحين تُستدعى `track_quest()`:

```sql
INSERT INTO quests (...)   ← مرفوض صامتاً
UPDATE quests SET ...      ← يطابق صفر صفوف
```

كتبتُ `FORCE` قاصداً منع الغش، فمنعتُ اللعبة نفسها من العمل.

## ٤. التحدّيات لا تتحرّك خارج صفحة الـHub

```js
function wireGameEvents() { … }   // hub_sm.js:325
wireGameEvents();                 // hub_sm.js:382 — داخل route('hub') فقط
```

إعجابك بمنشور في الفيد يبثّ `game:quests`… **ولا أحد يستمع**
لأن الـHub غير مفتوح.

## ٥. الرسائل مخفيّة تحت الشريط الجانبي

```css
.dm-list  { position: absolute; z-index: 51 }   /* تحت 1024px */
.nav-rail { z-index: 100 }                      /* أعلى — يغطّيها */
```

## ٦. الطيّ لا يعمل كما في ملفك

ملفك: `body.classList.toggle('side-folded')` — حالة مثبَّتة،
و`:hover` يكشف التسميات فقط.
عندي: `:hover` يوسّع الشريط **فوق** المحتوى. سلوك مختلف.

## ٧. لا يوجد نظام لغات إطلاقاً

```js
LOCALE: 'pref:locale',   // 'fr' | 'ar' | 'en'
```

المفتاح موجود، **ولا ملف ترجمة واحد**. كل النصوص فرنسية داخل الكود.

## ٨. لا إشعارات متصفح

`sw_sm.js` فيه `push` و`notificationclick` جاهزان، لكن **لا شيء يطلب
الإذن ولا يُنشئ إشعاراً**.

---

# الجزء الثاني · ما سأفعله

| # | العمل | الملفات |
|---|---|---|
| 1 | **تخطيط المحادثة** — ارتفاع صريح كأصلك · أحدث رسالة أولاً · لا زحزحة أثناء القراءة | `layout_sm.css` · `messages_sm.js` |
| 2 | **`db/08_fixes_sm.sql`** — سياسات كتابة للتحدّيات وXP · إعادة سياسة البروفايل · قاعدة الاسم | جديد |
| 3 | **رسالة خطأ صادقة** تنقل سبب Postgres الحقيقي | `profile_sm.js` · `api_sm.js` |
| 4 | **ربط اللعبة عند الإقلاع** لا عند فتح الـHub | `app_sm.js` · `hub_sm.js` |
| 5 | **الطبقات والطيّ** بمنطق ملفك | `layout_sm.css` · `shell_sm.js` |
| 6 | **`i18n_sm.js`** — EN افتراضية · FR · AR مع RTL · مبدّل أعلى اليمين | جديد + 12 وحدة |
| 7 | **`notify_sm.js`** — إذن في وقته · إشعار للرسائل والمتابعات | جديد + `sw_sm.js` |
| 8 | اختبارات لكل ما سبق | `tests/` |

## ضمانة تغيير الاسم — ١٥ يوماً

في القاعدة لا في المتصفح، وإلا التفّ عليها أي أحد:

```sql
ALTER TABLE profiles ADD COLUMN name_changed_at TIMESTAMPTZ;
ALTER TABLE profiles ADD COLUMN name_change_count INTEGER DEFAULT 0;
```

- الأول: يمرّ ويُسجَّل التاريخ
- الثاني خلال ١٥ يوماً: **تحذير يذكر كم يوماً بقي**، ثم يُسمح
- بعد ١٥ يوماً: العدّاد يُصفَّر تلقائياً

سأفترض *«تحذير ثم سماح»* — أقلّ إحباطاً وأصعب استغلالاً من المنع.
قل إن أردت المنع الفعلي.

---

## الترتيب

```
١  تخطيط المحادثة        ← أكثر ما يزعجك يومياً
٢  db/08_fixes_sm.sql    ← التحدّيات والحفظ لا يعملان بدونه
٣  رسالة الخطأ + ربط اللعبة
٤  الطبقات + الطيّ
٥  i18n  (الأكبر)
٦  إشعارات المتصفح
٧  اختبارات
```

---

## ما لن أفعله

- **لن أترجم آلياً.** العربية ستكون عربية طالب جزائري.
- **لن أطلب إذن الإشعارات عند الإقلاع.** بل بعد أول رسالة تصلك.
- **لن أقول «يعمل»** قبل أن أريك صفاً في Neon.

**قل «ابدأ».**
