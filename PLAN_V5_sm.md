# خطة V5 — الرسائل والبروفايل إلى جودة الإنتاج

> **ملاحظة على صياغة الطلب**
> طلبك الأصلي كان ممتازاً في التغطية، لكنه مكتوب بصفات:
> «pixel-perfect» · «production-ready» · «Discord level polish».
> الصفات لا تُختبَر — لا أستطيع إثبات أنني حقّقتها ولا أنت تستطيع
> رفضها بدليل. فأعدتُ كتابته إلى **معايير قابلة للقياس**، وفحصتُ
> الكود أولاً حتى تكون كل نقطة مبنية على خطأ حقيقي وجدته، لا على
> احتمال.

---

# الجزء الأول · ما فحصتُه فعلاً

## البروفايل — شكواك عن خلط اللغات مؤكَّدة بالحرف

```
public/js/features/profile_sm.js
  السطر 101:  <span>abonnés</span>          ← فرنسي ثابت
  السطر 102:  <span>abonnements</span>      ← فرنسي ثابت
  السطر 114:  'Abonné' · 'Se désabonner'    ← فرنسي ثابت
  السطر 387:  'Abonnés' : 'Abonnements'     ← فرنسي ثابت
  السطر  82:  '… Message'                    ← إنجليزي ثابت
```

ذكرتَ بالضبط: *"Posts, Media, Likes, No posts, Edit profile, Abonné,
abonnés"*. التبويبات أصلحتُها في الجولة الماضية، **والعدّادات لا.**

وإجمالاً ٣٠ نصاً ثابتاً في هذا الملف وحده، منها أسماء الكليات
العشرين — وهذه **لا يجب أن تُترجَم** (اسم الكلية بيانات، لا واجهة)،
لكن يجب أن تأتي من القاعدة لا من مصفوفة في الكود.

## العدّادات — تُحدَّث تفاؤلياً ولا تُصحَّح أبداً

```js
// السطر 269
if (f) f.textContent = compact((u.followers || 0) + (next === 'following' ? 1 : 0));
```

يزيد الرقم في الشاشة، **ولا يعيد قراءته من القاعدة أبداً**. فإن فشل
الطلب يبقى الرقم الخاطئ حتى تحديث الصفحة. وإنشاء منشور أو حذفه لا
يمسّ `stPosts` إطلاقاً.

## زر المراسلة — موجود، لكنه لا يفتح محادثة

```js
on($('#pfMessage'), 'click', () => go('messages', u.id));
```

ينتقل إلى `#/messages/u2` فقط. وطلبتَ صراحةً: *«opens or creates a DM
conversation instantly. If a conversation already exists, open it;
otherwise create one»*. الفرق حقيقي: إن لم توجد محادثة، تصل إلى لوح
فارغ لا إلى محادثة جديدة.

## النوافذ الطافية — تُقصّ عمودياً

```
gif_sm.js :  innerWidth = 1 مرة   ·  innerHeight = 0 مرة   ← لا حدّ عمودي
ui_sm.js  :  innerWidth = 2 مرة   ·  innerHeight = 1 مرة   ← ناقص
```

فمنتقي GIF يُقصّ من الأسفل إن فُتح قرب حافة الشاشة السفلى — وهو
المكان الطبيعي لزرّه في شريط الكتابة.

## قفزة التخطيط

```js
host.innerHTML = `<div id="pfRoot">${skeletonList(3)}</div>`;
```

الهيكل العظمي **بارتفاع مختلف** عن رأس البروفايل الحقيقي (غلاف
١٤٨px + صورة ٩٦px + تبويبات). فالصفحة تقفز عند وصول البيانات.

---

# الجزء الثاني · معايير القبول

كل بند أدناه له **فحص آلي**. لن أقول «تمّ» بلا رقم.

## أ · الرسائل

| # | المعيار | كيف يُقاس |
|---|---|---|
| A1 | لا حالة فارغة إلا حين لا توجد بيانات فعلاً | فتح `/messages` → عدد الفقاعات > 0 |
| A2 | المحادثة تملأ الارتفاع المتاح | `threadBody.clientHeight > 0` وسلسلة الارتفاع كاملة |
| A3 | لا تمرير أفقي في أي عرض | `scrollWidth <= clientWidth` عند 360 · 768 · 1024 · 1440 |
| A4 | النوافذ الطافية داخل الشاشة | كل `rect` ضمن الحدود الأربعة، عند كل حافة |
| A5 | النوافذ فوق كل شيء | z-index أعلى من اللوح والشريط |
| A6 | فراغات موحّدة | كل قيم padding من `--s*` لا أرقام سائبة |
| A7 | أحجام الصور موحّدة | `.av` بأحجامها الخمسة فقط، بلا تجاوزات inline |
| A8 | لا وميض | نفس عقدة DOM بعد استطلاع (مُختبَر بالفعل) |

## ب · البروفايل

| # | المعيار | كيف يُقاس |
|---|---|---|
| B1 | صفر نص واجهة غير مترجَم | مسح الشاشة بالإنجليزية والعربية |
| B2 | العدّادات من القاعدة، وتُصحَّح | متابعة → فشل مُتعمَّد → الرقم يعود |
| B3 | العدّاد يتحرّك فوراً بعد المتابعة | بلا تحديث صفحة |
| B4 | زر المراسلة يفتح محادثة فعلاً | محادثة جديدة تُنشأ إن لم توجد |
| B5 | الأزرار الصحيحة للشخص الصحيح | صاحب الحساب: تعديل · الزائر: متابعة + مراسلة |
| B6 | الإحصاءات قابلة للنقر | متابِعون/يتابع → قائمة · منشورات → التبويب |
| B7 | لا قفزة تخطيط | ارتفاع الهيكل = ارتفاع المحتوى ± 8px |
| B8 | الغلاف والصورة لا يتداخلان | عند 360 · 768 · 1440 |

## ج · مشترك

| # | المعيار |
|---|---|
| C1 | تباين الوضع الداكن ≥ 4.5:1 للنص، ≥ 3:1 للحدود |
| C2 | حواف ومسافات موحّدة عبر كل الألواح |
| C3 | `prefers-reduced-motion` يوقف كل الحركات |
| C4 | كل زر أيقونة له `aria-label` مترجَم |
| C5 | هدف اللمس ≥ 44×44 على الشاشات الصغيرة |

---

# الجزء الثالث · ما لن أفعله

- **لن أعيد التصميم.** طلبتَ الإصلاح والصقل فقط، وهذا ما سيحدث.
- **لن أقول «pixel-perfect».** jsdom لا يرسم بكسلات؛ سأقيس ما يمكن
  قياسه (تصريحات، أحجام، حدود، z-index) وأقول بوضوح ما لم أقِسه.
- **لن أترجم أسماء الكليات.** «Informatique» اسم كلية جزائرية، لا
  نص واجهة. سأنقلها إلى القاعدة بدل تركها مصفوفة في الكود.
- **لن أخترع «Discord-level».** سآخذ منها ثلاثة أنماط محدّدة:
  عرض ثابت للوح المحادثات · فقاعات مجمّعة بلا تكرار الاسم ·
  فاصل تاريخ لاصق.

---

# الترتيب

```
١  الرسائل: التخطيط · الطفو · الفراغات      ← ما تراه كل يوم
٢  البروفايل: اللغة · العدّادات · المراسلة
٣  الاستجابة: 360 → 1440
٤  الوضع الداكن والحركة
٥  اختبارات لكل معيار أعلاه
```

**سأبدأ فوراً بعد كتابة هذا.**
