# خطة V7 — المشاكل الخمس، واحدة واحدة

فحصتُ كلاً منها قبل الكتابة. **وجدتُ خطأ صياغة CSS من صنعي** يفسّر
شكواك عن صفحة الرسائل.

---

# ١ · صفحة الرسائل لا تظهر حتى تضغط الفراغ

## السبب: قاعدة CSS مكسورة تركها سكربتي

```css
.dm,                              ← السطر 2370
                                  ← محدِّد معلّق بلا كتلة
@media (min-width: 1400px) {
```

فاصلة تنتظر محدِّداً ثانياً، فيليها `@media`. **هذا خطأ صياغة**:
المتصفح يتجاهل قاعدة `.dm` **ويبتلع الـ`@media` كاملاً معها**.

فتفقد `.dm` ارتفاعها الصريح، ولا تظهر اللوحة حتى يُجبَر المتصفح على
إعادة حساب التخطيط — وهو ما يحدث حين تضغط في الفراغ.

**سببتُه أنا** في سكربت حذف الطيّ: أزال شطراً من قائمة محدِّدات وترك
الفاصلة. وفحصتُ باقي الملفات: **هذه الوحيدة**.

## الإصلاح
- إصلاح القاعدة
- **وفحص صياغة CSS دائم في الاختبارات** — لا يوجد اليوم، ولو وُجد
  لأمسك هذا فوراً

---

# ٢ · المقاسات تصغر عند تقسيم الشاشة

## السبب: نقاط توقّف تقفز، لا مقاسات تسيل

```
clamp() مستخدمة  ٥ مرات فقط
--feed-w         720px ثابتة
```

عرض ثابت + عشر نقاط توقّف = الصفحة تبقى بحجمها حتى تعبر عتبة، ثم
**تقفز**. وبين العتبتين يظهر فراغ أو تنضغط الأعمدة.

## الإصلاح
- `--feed-w` تصبح `clamp(320px, 92vw, 780px)` — تسيل مع النافذة
- الفراغات الكبيرة تصبح `clamp()` بدل قيم ثابتة
- الخطوط الكبيرة (العناوين، الأرقام) تسيل مع `clamp()`
- **`container queries` حيث تنفع**: لوحة المحادثة تستجيب لعرضها هي،
  لا لعرض النافذة — وهذا بالضبط ما يكسر في وضع تقسيم الشاشة

---

# ٣ · الرسائل يجب أن تُفتح كإنستغرام

طلبتَ: ضغطة على «Messages» → قائمة الـDM مباشرة، ثم ضغطة على محادثة
→ أحدث رسالة.

## الوضع الحالي
اختبرتُ التوجيه فعلياً — **يعمل في jsdom**:

```
الضغط على Messages       → #/messages · اللوحة تُرسم · ٣ فقاعات
الضغط عليها وأنت في محادثة → يعود للقائمة
```

فالمشكلة **ليست في التوجيه** بل في القاعدة المكسورة أعلاه: اللوحة
تُرسم لكن بلا ارتفاع.

## الإصلاح
- إصلاح ١ يحلّ الأساس
- **وأضيف**: الضغط على «Messages» وأنت **داخل محادثة** يعود إلى
  القائمة على الشاشات الضيّقة (سلوك إنستغرام)، ويُبقي المحادثة
  مفتوحة على الشاشات العريضة (سلوك إنستغرام على الويب أيضاً)

---

# ٤ · GIF والتسجيل الصوتي يفتحان للأسفل

## السبب: الانقلاب مشروط، لا افتراضي

```js
const openUp = spaceBelow < r.height + pad && spaceAbove > spaceBelow;
```

ينقلب **فقط** إن ضاقت المساحة تحت. لكن زر GIF في شريط الكتابة —
**أسفل الشاشة دائماً** — فالمكان الطبيعي للوحة هو الأعلى، لا الأسفل.

## الإصلاح
- منتقي GIF يفتح **لأعلى افتراضياً** من شريط الكتابة، وينقلب للأسفل
  فقط إن لم تكن هناك مساحة فوق
- **والتسجيل الصوتي**: يظهر الآن مكان شريط الكتابة نفسه
  (`#composerWrap.recording .composer { display: none }`) — سأجعله
  لوحة تعلو الشريط بدل أن تستبدله، فلا يختفي ما كنتَ تكتبه

---

# ٥ · إشعار عند إنجاز تحدٍّ

## السبب: موجود، لكنه فرنسي ثابت وباهت

```js
toast(`Défi accompli · ${label}` + …)   ← نصّ فرنسي رغم كل الترجمة
```

مرّ من مسح الترجمة لأنه داخل قالب نصّي.

## الإصلاح — وهذا الذي طلبته فعلاً
بطاقة إنجاز حقيقية، لا toast عادي:

```
✓ علامة صحّ متحرّكة ترتسم
  اسم التحدي مشطوباً
  «+8 XP» يعدّ تصاعدياً
  «تبقّى تحدٍّ واحد» أو «اكتمل اليوم!»
  شريط تقدّم اليوم ٢/٣ → ٣/٣
```

ومع اكتمال اليوم: احتفال أكبر + الـstreak يزيد أمام عينك.
**وكلها مترجَمة**.

---

# الترتيب

```
١  إصلاح CSS المكسورة + فحص صياغة دائم   ← يحلّ شكواك الأساسية
٢  المقاسات السائلة + container queries
٣  سلوك التنقّل كإنستغرام
٤  اتجاه GIF والتسجيل
٥  بطاقة الإنجاز
٦  اختبارات لكل ما سبق
```

## ما لن أفعله
- **لن أدّعي التحقّق من البكسل.** jsdom لا يحسب تخطيطاً؛ سأفحص
  التصريحات وصحّة الصياغة، وأقول بوضوح ما لم أقِسه.

**أبدأ الآن.**
