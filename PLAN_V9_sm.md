# PLAN V9 — الصفحات الناقصة في الهاتف
**قبل التنفيذ.**

---

## 1. ما قِسته فعلاً (لا تخمين)

شغّلت Chrome حقيقياً على 360px و 412px وقرأت كل عنصر في الشريط السفلي:

```
412px: rail=412  عناصر ظاهرة=6
   feed  explore  messages  notifications  hub  profile     (64px لكل واحد)
360px: rail=360  عناصر ظاهرة=6                               (55px لكل واحد)
```

### تصحيح: كنت مخطئاً في ملخّصي السابق
قلت سابقاً إن `[data-nav="hub"]` مخفي على الهاتف بسبب «حدّ 5 تبويبات».
**هذا غير صحيح.** بحثت عن قاعدة CSS تخفيه فلم أجد أي واحدة، والقياس
يُظهر Hub ظاهراً بعرض 64px. الـ Hub موجود ويعمل.

### المشكلة الحقيقية: 4 صفحات لا يمكن الوصول إليها إطلاقاً من الهاتف

القاعدة في `layout_sm.css:620`:
```css
.nav-item:not(.primary) { display: none; }   /* only 5 core items fit */
```

والعناصر التي ليست `.primary`:

| الصفحة | الحالة على الهاتف | هل الكود موجود؟ |
|---|---|---|
| **events** | `display:none` — لا رابط، لا اختصار | نعم، `campus_sm.js` كامل مع `openEventComposer` و `eventCard` و `attend()` و XP |
| **qa** | `display:none` | نعم، كامل مع `openAsk` و `openQuestion` |
| channels | `display:none` | نعم |
| saved | `display:none` | نعم |

الصفحات مبنية بالكامل ومربوطة بالراوتر (`initCampus` يسجّل `route()` لكل
واحدة). المفقود هو **الطريق إليها فقط**. على الحاسوب تظهر في الشريط
الجانبي؛ على الهاتف لا يوجد أي مدخل.

قلت: «الصفحات مهمة هي events و qa» — وهذا بالضبط ما تحقّق.

---

## 2. لماذا لن أضيفها إلى الشريط السفلي

6 تبويبات الآن = 55px لكل واحد على 360px. إضافة events و qa تعني
8 تبويبات = **41px** لكل واحد. الحد الأدنى لمنطقة اللمس في إرشادات
أندرويد هو 48dp. سيصبح الشريط غير قابل للاستعمال، وستكون هذه شكوى
الأسبوع القادم.

---

## 3. الاقتراح

### أ) Events و Q&A ← تبويبان في **Explore**

`explore` هو أصلاً شاشة اكتشاف من نفس الملف (`campus_sm.js`) وله نفس
شكل `mountScreen`. أضيف شريط `sub-tabs` أعلى Explore — **نفس المكوّن**
المستعمل في الصفحة الرئيسية (`.sub-tabs` / `.sub-tab`)، فلا CSS جديد
ولا نمط بصري جديد:

```
Explore:  [ الناس ]  [ الفعاليات ]  [ أسئلة وأجوبة ]  [ القنوات ]  [ المحفوظات ]
```

- `#/events` و `#/qa` تبقى روابط صالحة (لا تنكسر الإشعارات ولا الروابط
  المحفوظة) — تفتح Explore على التبويب الصحيح
- على الحاسوب يبقى الشريط الجانبي كما هو، ويظهر شريط التبويبات أيضاً
  فيتطابق السلوك بدل أن يكون لكل حجم منطق مختلف
- التبويب المختار يُحفظ في الـ hash فيصمد أمام F5 وزر الرجوع

### ب) Hub ← بطاقة تقدّم في الصفحة الرئيسية **أيضاً**

طلبت هذا صراحة. Hub سيبقى تبويباً (هو موجود ويعمل)، لكن أضيف فوق قائمة
المنشورات بطاقة مدمجة:

```
┌──────────────────────────────────┐
│  المستوى 7   ▓▓▓▓▓▓▓░░░  340/520 │
│  🔥 12 يوم                        │
│  ✓ نشرت منشوراً      ✓ +8         │
│  ○ علّق على 3         1/3         │
│  ○ ادخل غداً                      │
└──────────────────────────────────┘
```

- تُقرأ من `game_sm.js` الموجود — **لا SQL جديد، لا جدول جديد**
- قابلة للطي، والحالة محفوظة في `prefs`
- الضغط عليها ينقل إلى `#/hub` الكامل
- تظهر فقط في تبويب «لك» من الصفحة الرئيسية، لا في «المتابَعون»، حتى لا
  تتكرّر في كل تبويب

---

## 4. ما لن أفعله
- لن أضيف تبويبات للشريط السفلي (السبب أعلاه)
- لن أخفي شيئاً موجوداً الآن
- لن ألمس قاعدة البيانات

## 5. التحقّق الذي سأقوم به
- قياس في Chrome حقيقي على 360 / 412 / 768 / 1280
- التأكد أن `#/events` و `#/qa` ما زالا يعملان كروابط مباشرة
- اختبار انحدار جديد + تشغيل 851 + 181 + 34
- **صور PNG أنظر إليها بنفسي** بالعربية والإنجليزية
- APK جديد بـ versionCode مرفوع (1.3)

---

## سؤال واحد قبل أن أبدأ
هل ترتيب تبويبات Explore يناسبك: **الناس · الفعاليات · أسئلة · القنوات ·
المحفوظات**؟ أم تريد الفعاليات والأسئلة أولاً لأنهما الأهم عندك؟
