# تجربة التسجيل والدخول — بلا حساب أدمن

> ✅ **تحققت للتو من السلسلة كاملة على مشروعك الحيّ:**
> إنشاء حساب → JWT → صف في جدول `profiles`. كلها تعمل.
> تفعيلك لـ Email & Password نجح.

---

## المشكلة الوحيدة

المخطط يضع كل حساب جديد على `status = 'pending'`، وسياسات RLS تمنع
الطالب من الموافقة على نفسه — **اختبرت ذلك وحصلت على `42501`**، وهذا
هو السلوك الصحيح.

لكنه يعني أنك لا تستطيع الاختبار بلا أدمن. الحل ملف واحد.

---

## الخطوة 1 — شغّل ملف الاختبار

**Neon Console → SQL Editor** → الصق **`db/04_testing_sm.sql`** → **Run**

ماذا يفعل:
- يجعل الحسابات الجديدة `approved` تلقائياً
- يوافق على كل من ينتظر حالياً
- **يضيف 5 طلاب تجريبيين** ليمتلئ الترتيب

> الملف يحوي في نهايته أوامر **التراجع** — شغّلها قبل فتح التطبيق لطلاب حقيقيين.

---

## الخطوة 2 — شغّل الخادم المحلي

```bash
cd /home/user/koliya/public
python3 -m http.server 8099
```

افتح: **http://localhost:8099/index_sm.html**

> ⚠️ لا تفتح الملف بنقرة مزدوجة (`file://`) — وحدات ES تحتاج `http://`.

---

## الخطوة 3 — تأكّد من النطاق الموثوق

**Neon Console → Auth → Trusted domains** → أضف:
```
http://localhost:8099
```
بدونه ستحصل على `MISSING_ORIGIN` بدل رسالة خطأ مفهومة.

---

## الخطوة 4 — سجّل حساباً

اضغط **"Créer un compte"** واملأ:

| الحقل | مثال |
|---|---|
| Carte étudiant | `CS-042` |
| Nom complet | `Sara Benali` |
| Nom d'utilisateur | `sara.b` |
| Email | `sara@gmail.com` |
| Faculté | Informatique |
| Mot de passe | 8 أحرف على الأقل |

**تحقّق فوراً في Neon:**
```sql
SELECT student_card, username, full_name, email, faculty, status, role, created_at
FROM profiles ORDER BY created_at DESC LIMIT 5;
```

يجب أن ترى صفّك الجديد.

**وسجّل الخروج ثم ادخل** ببطاقتك وكلمة المرور للتأكد من الدورة كاملة.

---

## الخطوة 5 — اجعل نفسك أدمن (اختياري)

```sql
UPDATE profiles SET role = 'admin', status = 'approved'
WHERE upper(student_card) = upper('CS-042');
```

---

## ما تجربه بعد الدخول

| الشاشة | جرّب |
|---|---|
| **Accueil** | انقر منشوراً · **نقر مزدوج على صورة** = إعجاب · `J`/`K`/`L` |
| **Messages** | انقر فقاعة → الأدوات · الهيدر → لوحة الإنفو |
| **Hub** | التحديات · الشارات · الشعلة |
| **Classement** ⭐ | جديد — منصّة، فلاتر، موضعك مثبّت أسفل |
| **Profil** | مرّر → الغلاف يتقلّص |

**اختصارات:** `Ctrl+K` · `G` ثم `H` · `N` · `Échap`

---

## أخطاء محتملة

| الرسالة | السبب |
|---|---|
| `Domaine non autorisé` | أضف `http://localhost:8099` في Trusted domains |
| `Connexion par email désactivée` | فعّل Email & Password |
| `Compte en attente` | لم تشغّل `04_testing_sm.sql` |
| `carte déjà utilisée` | بطاقة مستعملة — جرّب رقماً آخر |
| صفحة بيضاء | فتحت بـ`file://` بدل `http://` |
| الشبكة تفشل | افتح Console (F12) وأرسل لي الخطأ |

---

## ⚠️ ملاحظتان مهمتان

**١. الصور ما زالت تجريبية** — R2 غير مُعدّ بعد. المنشورات والرسائل
والبروفايلات ستعمل بقاعدة البيانات، لكن رفع الصور لا.

**٢. الوحدات لم تُربط بعد** — التسجيل والدخول يكتبان في Neon فعلاً،
لكن الفيد والرسائل ما زالت تعرض بيانات تجريبية. الربط هو خطوتي التالية
بعد أن تؤكّد أن الدخول يعمل عندك.
